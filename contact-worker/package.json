{
  "name": "mint-contact-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "test:browser": "playwright test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "wrangler": "4.131.1"
  }
}
