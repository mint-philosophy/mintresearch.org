---
import BaseLayout from '../layouts/BaseLayout.astro';
import Section from '../components/Section.astro';
import RefTable from '../components/RefTable.astro';
---

<BaseLayout title="Agentic Research Infrastructure for AI Alignment, Governance and Adaptation">

<main>

<!-- ============ HERO ============ -->
<section class="hero" id="overview">
  <h1 data-typing="❯ Agentic Research Infrastructure for AI Alignment, Governance and Adaptation"><span class="typing-cursor"></span></h1>
</section>

<!-- ============ PIPELINE ============ -->
<section id="pipeline" class="fade-in no-brackets">

  <div class="pipeline-wrap">
    <svg viewBox="0 0 960 710" width="100%" xmlns="http://www.w3.org/2000/svg" style="font-family:'JetBrains Mono',monospace">
      <defs>
        <marker id="arrowBlue" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6" fill="#22d3ee"/></marker>
        <marker id="arrowGreen" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6" fill="#2ec4b6"/></marker>
        <marker id="arrowOrange" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6" fill="#f59e0b"/></marker>
        <marker id="arrowPurple" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6" fill="#a78bfa"/></marker>
        <marker id="arrowTeal" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6" fill="#2dd4bf"/></marker>
        <marker id="arrowPink" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6" fill="#f472b6"/></marker>
        <linearGradient id="nodeGrad" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stop-color="#1a1f2c"/><stop offset="100%" stop-color="#13171f"/></linearGradient>
        <filter id="glow"><feGaussianBlur stdDeviation="3" result="blur"/><feFlood flood-color="#2ec4b6" flood-opacity="0.3" result="color"/><feComposite in="color" in2="blur" operator="in" result="colorBlur"/><feMerge><feMergeNode in="colorBlur"/><feMergeNode in="SourceGraphic"/></feMerge></filter>
      </defs>

      <!-- ===== ROW 0: Horizontal input chain ===== -->
      <g class="pn" data-tip="Six input sources: Twitter/X, arXiv preprints, RSS feeds, Substack newsletters, Bluesky, and email newsletters. Hundreds of new items appear daily." style="cursor:pointer" transform="translate(20,15)">
        <rect width="135" height="48" fill="url(#nodeGrad)" stroke="rgba(255,255,255,0.1)"/>
        <text x="68" y="18" text-anchor="middle" fill="#94a3b8" font-size="9" font-weight="600">EXTERNAL SOURCES</text>
        <text x="68" y="32" text-anchor="middle" fill="#e2e8f0" font-size="9">Twitter, arXiv, RSS, Substack</text>
        <text x="68" y="43" text-anchor="middle" fill="#e2e8f0" font-size="9">Bluesky, Email</text>
      </g>
      <line x1="155" y1="39" x2="175" y2="39" stroke="#22d3ee" stroke-width="1.5" marker-end="url(#arrowBlue)"/>

      <g class="pn" data-tip="Scheduled daemon that fetches from all sources, deduplicates against a 400-item cache, then scores each item with Claude Opus across six weighted dimensions — Relevance (.30), Quality (.25), Authority (.15), Novelty (.15), Actionability (.10), Engagement (.05) — against a ~2,200-word research interest profile covering AI safety, governance, agent engineering, political economy, and more. Items above the composite threshold are ranked and posted to Slack." style="cursor:pointer" transform="translate(183,18)">
        <rect width="120" height="42" fill="url(#nodeGrad)" stroke="#22d3ee" stroke-width="1.5"/>
        <text x="60" y="18" text-anchor="middle" fill="#22d3ee" font-size="10" font-weight="700">feedme-daily</text>
        <text x="60" y="32" text-anchor="middle" fill="#94a3b8" font-size="8">Fetch, Classify, Rank</text>
      </g>
      <line x1="303" y1="39" x2="323" y2="39" stroke="#22d3ee" stroke-width="1.5" marker-end="url(#arrowBlue)"/>

      <g class="pn" data-tip="Daily Slack channels (#feed-monday, #feed-tuesday, etc.) where classified items land. Seth reviews these and thumbs-up the ones worth promoting." style="cursor:pointer" transform="translate(331,18)">
        <rect width="95" height="42" fill="#0f1520" stroke="rgba(255,255,255,0.1)"/>
        <text x="48" y="18" text-anchor="middle" fill="#fbbf24" font-size="10" font-weight="700">#feed-{'{'}day{'}'}</text>
        <text x="48" y="32" text-anchor="middle" fill="#94a3b8" font-size="8">Slack channel</text>
      </g>
      <line x1="426" y1="39" x2="446" y2="39" stroke="#fbbf24" stroke-width="1" stroke-dasharray="3,3"/>

      <g class="pn" data-tip="Human-in-the-loop curation. Seth reviews classified items and reacts with thumbs-up to promote them through the pipeline. This is the only manual step." style="cursor:pointer" transform="translate(449,22)">
        <rect width="80" height="34" fill="url(#nodeGrad)" stroke="#fbbf24" stroke-dasharray="4,3"/>
        <text x="40" y="22" text-anchor="middle" fill="#fbbf24" font-size="10">Seth &#x1F44D;</text>
      </g>
      <line x1="529" y1="39" x2="549" y2="39" stroke="#2ec4b6" stroke-width="1.5" marker-end="url(#arrowGreen)"/>

      <g class="pn" data-tip="Watches #feed channels for Seth's thumbs-up reactions every 5 minutes. Copies promoted messages verbatim to #firehose, including validated PDF attachments from thread replies." style="cursor:pointer" transform="translate(557,18)">
        <rect width="120" height="42" fill="url(#nodeGrad)" stroke="#2ec4b6" stroke-width="1.5"/>
        <text x="60" y="18" text-anchor="middle" fill="#2ec4b6" font-size="10" font-weight="700">feed-promote</text>
        <text x="60" y="32" text-anchor="middle" fill="#94a3b8" font-size="8">Copy to #firehose</text>
      </g>

      <!-- feed-promote arrow DOWN to hub -->
      <line x1="617" y1="60" x2="617" y2="85" stroke="#2ec4b6" stroke-width="1.5"/>
      <line x1="617" y1="85" x2="510" y2="85" stroke="#2ec4b6" stroke-width="1.5"/>
      <line x1="510" y1="85" x2="510" y2="105" stroke="#2ec4b6" stroke-width="1.5" marker-end="url(#arrowGreen)"/>

      <!-- ===== ROW 1: Two writers -> #firehose hub ===== -->
      <g class="pn" data-tip="Direct sharing from any device — iPhone share sheet, Mac hotkey (Cmd+Shift+M), Chrome extension, or manual paste. Bypasses automated classification with a score override of 1.0." style="cursor:pointer" transform="translate(30,108)">
        <rect width="140" height="42" fill="url(#nodeGrad)" stroke="#fbbf24" stroke-dasharray="4,3"/>
        <text x="70" y="17" text-anchor="middle" fill="#fbbf24" font-size="9" font-weight="600">SETH'S DEVICES</text>
        <text x="70" y="32" text-anchor="middle" fill="#e2e8f0" font-size="9">iPhone, Mac, Chrome</text>
      </g>
      <line x1="170" y1="129" x2="195" y2="129" stroke="#f59e0b" stroke-width="1.5" marker-end="url(#arrowOrange)"/>

      <g class="pn" data-tip="Real-time intake daemon via Slack Socket Mode. When Seth shares a URL from any device, it immediately classifies, enriches with metadata and PDFs, and posts to #firehose." style="cursor:pointer" transform="translate(203,108)">
        <rect width="130" height="42" fill="url(#nodeGrad)" stroke="#f59e0b" stroke-width="1.5"/>
        <text x="65" y="17" text-anchor="middle" fill="#f59e0b" font-size="10" font-weight="700">share-to-minty</text>
        <text x="65" y="32" text-anchor="middle" fill="#94a3b8" font-size="8">Classify &amp; post to #firehose</text>
      </g>
      <line x1="333" y1="129" x2="440" y2="129" stroke="#f59e0b" stroke-width="1.5" marker-end="url(#arrowOrange)"/>

      <!-- #firehose central hub -->
      <g class="pn" data-tip="The central hub channel. All curated content converges here — both automated (via feed-promote) and manual (via share-to-minty). Three downstream daemons read from this channel." style="cursor:pointer" transform="translate(440,105)">
        <rect width="140" height="65" fill="#0f1520" stroke="#22d3ee" stroke-width="2" filter="url(#glow)"/>
        <text x="70" y="25" text-anchor="middle" fill="#22d3ee" font-size="13" font-weight="800">#firehose</text>
        <text x="70" y="42" text-anchor="middle" fill="#94a3b8" font-size="9">Automated Hub</text>
        <text x="70" y="55" text-anchor="middle" fill="#64748b" font-size="9">2 writers / 3 readers</text>
      </g>

      <!-- ===== RIGHT COLUMN: Digest loop ===== -->
      <line x1="580" y1="130" x2="640" y2="130" stroke="#f472b6" stroke-width="1.5" marker-end="url(#arrowPink)"/>

      <g class="pn" data-tip="Generates a narrative daily digest from the previous day's #firehose posts using Claude. Sends as a rich HTML email to 29 lab members across 11 institutions and posts to #mint-community." style="cursor:pointer" transform="translate(648,108)">
        <rect width="150" height="50" fill="url(#nodeGrad)" stroke="#f472b6" stroke-width="1.5"/>
        <text x="75" y="19" text-anchor="middle" fill="#f472b6" font-size="10" font-weight="700">yesterday-in-ai</text>
        <text x="75" y="33" text-anchor="middle" fill="#94a3b8" font-size="8">Daily/weekly digest</text>
        <text x="75" y="44" text-anchor="middle" fill="#64748b" font-size="8">#mint-community + email</text>
      </g>

      <line x1="798" y1="133" x2="838" y2="133" stroke="#f472b6" stroke-width="1" marker-end="url(#arrowPink)"/>
      <g class="pn" data-tip="Rich HTML email digest sent daily to 29 lab members across 11 institutions, including MINT Lab members, collaborators at Stanford, ANU, and elsewhere." style="cursor:pointer" transform="translate(846,114)">
        <rect width="100" height="38" fill="url(#nodeGrad)" stroke="rgba(255,255,255,0.1)"/>
        <text x="50" y="16" text-anchor="middle" fill="#f472b6" font-size="9" font-weight="600">Email</text>
        <text x="50" y="30" text-anchor="middle" fill="#64748b" font-size="8">29 lab members</text>
      </g>

      <line x1="723" y1="158" x2="723" y2="200" stroke="#f472b6" stroke-width="1.5" marker-end="url(#arrowPink)"/>
      <text x="755" y="183" fill="#f472b6" font-size="7" opacity=".85" font-style="italic">daily digest</text>

      <g class="pn" data-tip="The lab's human discussion channel. Receives the daily digest, and lab members discuss, ask questions, and share reactions. share-to-minty also watches this channel." style="cursor:pointer" transform="translate(633,208)">
        <rect width="180" height="58" fill="#0f1520" stroke="#2ec4b6" stroke-width="1.5"/>
        <text x="90" y="18" text-anchor="middle" fill="#2ec4b6" font-size="11" font-weight="700">#mint-community</text>
        <text x="90" y="33" text-anchor="middle" fill="#94a3b8" font-size="8">Human discussion channel</text>
        <text x="90" y="46" text-anchor="middle" fill="#64748b" font-size="8">Daily digest + Seth &amp; lab Q&amp;A</text>
      </g>
      <g class="pn" data-tip="Lab members discuss the daily digest, ask questions about papers, and share their own finds. Discussion threads loop back into the pipeline via share-to-minty." style="cursor:pointer" transform="translate(846,218)">
        <rect width="100" height="38" fill="url(#nodeGrad)" stroke="#fbbf24" stroke-dasharray="4,3"/>
        <text x="50" y="16" text-anchor="middle" fill="#fbbf24" font-size="9" font-weight="600">Lab Members</text>
        <text x="50" y="30" text-anchor="middle" fill="#64748b" font-size="8">Discussion &amp; Q&amp;A</text>
      </g>
      <line x1="846" y1="237" x2="813" y2="237" stroke="#fbbf24" stroke-width="1" stroke-dasharray="3,3" marker-end="url(#arrowOrange)"/>

      <text x="723" y="276" text-anchor="middle" fill="#f59e0b" font-size="7" opacity=".75" font-style="italic">share-to-minty also watches</text>

      <line x1="633" y1="245" x2="605" y2="245" stroke="#2ec4b6" stroke-width="1.5"/>
      <line x1="605" y1="245" x2="605" y2="152" stroke="#2ec4b6" stroke-width="1.5"/>
      <line x1="605" y1="152" x2="580" y2="152" stroke="#2ec4b6" stroke-width="1.5" marker-end="url(#arrowGreen)"/>

      <!-- ===== ROW 2: Paper pipeline ===== -->
      <line x1="510" y1="170" x2="510" y2="225" stroke="#a78bfa" stroke-width="1.5" marker-end="url(#arrowPurple)"/>

      <g class="pn" data-tip="Polls #firehose every 5 minutes for URLs without attached PDFs. Tries programmatic download (arXiv, bioRxiv, OpenReview, direct links), then falls back to a sandboxed Claude agent for harder cases." style="cursor:pointer" transform="translate(440,228)">
        <rect width="140" height="50" fill="url(#nodeGrad)" stroke="#a78bfa" stroke-width="1.5"/>
        <text x="70" y="19" text-anchor="middle" fill="#a78bfa" font-size="10" font-weight="700">news-pdf-bot</text>
        <text x="70" y="33" text-anchor="middle" fill="#94a3b8" font-size="8">Download PDFs + markdown</text>
        <text x="70" y="44" text-anchor="middle" fill="#64748b" font-size="8">+ date markers</text>
      </g>
      <line x1="510" y1="278" x2="510" y2="303" stroke="#a78bfa" stroke-width="1.5" marker-end="url(#arrowPurple)"/>

      <g class="pn" data-tip="Slack channel where downloaded PDFs and conversion notifications are posted. Papers land here with metadata before being picked up by the corpus ingestion pipeline." style="cursor:pointer" transform="translate(450,306)">
        <rect width="120" height="38" fill="#0f1520" stroke="#fbbf24" stroke-width="1"/>
        <text x="60" y="15" text-anchor="middle" fill="#fbbf24" font-size="10" font-weight="700">#papers</text>
        <text x="60" y="30" text-anchor="middle" fill="#94a3b8" font-size="8">PDFs + notifications</text>
      </g>
      <line x1="510" y1="344" x2="510" y2="370" stroke="#2dd4bf" stroke-width="1.5" marker-end="url(#arrowTeal)"/>

      <g class="pn" data-tip="Unified 11-stage ingestion pipeline: download, rename, markdown conversion (6-tier cascade), embed (Voyage 2048d), enrich (CrossRef/Semantic Scholar), analyze (41 questions via LLM), RAPTOR summaries, verify, Drive upload, Zotero catalog, Slack report." style="cursor:pointer" transform="translate(440,373)">
        <rect width="140" height="50" fill="url(#nodeGrad)" stroke="#2dd4bf" stroke-width="1.5"/>
        <text x="70" y="19" text-anchor="middle" fill="#2dd4bf" font-size="10" font-weight="700">corpus-ingest</text>
        <text x="70" y="33" text-anchor="middle" fill="#94a3b8" font-size="8">Unified pipeline</text>
        <text x="70" y="44" text-anchor="middle" fill="#64748b" font-size="8">10-stage ingestion</text>
      </g>
      <line x1="510" y1="423" x2="510" y2="470" stroke="#2dd4bf" stroke-width="1.5" marker-end="url(#arrowTeal)"/>

      <g class="pn" data-tip="Vector database holding ~2,200 papers with 48 embedding dimensions per document (each 2048d via Voyage), 154K text chunks, 41 semantic analysis dimensions, 29 topic clusters, and 11,570 pre-computed similarity pairs." style="cursor:pointer" transform="translate(435,473)">
        <rect width="150" height="52" fill="url(#nodeGrad)" stroke="#2dd4bf" stroke-width="1.5"/>
        <text x="75" y="17" text-anchor="middle" fill="#2dd4bf" font-size="10" font-weight="700">LanceDB Corpus</text>
        <text x="75" y="31" text-anchor="middle" fill="#94a3b8" font-size="8">~2,200 papers</text>
        <text x="75" y="43" text-anchor="middle" fill="#64748b" font-size="8">41 semantic dimensions</text>
      </g>

      <line x1="510" y1="525" x2="510" y2="555" stroke="#22d3ee" stroke-width="1.5" marker-end="url(#arrowBlue)"/>

      <g class="pn" data-tip="Slack bot (@Minty) offering 7 search modes: fast search, deep review, catch me up, semantic similarity, dimension search, chunk search, and unified multi-modal search with Reciprocal Rank Fusion." style="cursor:pointer" transform="translate(415,558)">
        <rect width="190" height="52" fill="url(#nodeGrad)" stroke="#22d3ee" stroke-width="1.5"/>
        <text x="95" y="17" text-anchor="middle" fill="#22d3ee" font-size="10" font-weight="700">corpus-agent</text>
        <text x="95" y="31" text-anchor="middle" fill="#94a3b8" font-size="8">@Minty &#x2022; 7 modes &#x2022; News Search</text>
        <text x="95" y="43" text-anchor="middle" fill="#64748b" font-size="8">Deep Review &#x2022; Catch Me Up &#x2022; more</text>
      </g>

      <!-- ===== BOTTOM-LEFT: Background services ===== -->
      <g class="pn" data-tip="Always-on infrastructure: minty-search (Slack semantic search), slack-index (message indexing every 4h), workspace-index (file indexing every 6h), rclone-sync (Drive backups), and cli-health-probe (daemon monitoring)." style="cursor:pointer" transform="translate(20,460)">
        <rect width="180" height="190" fill="url(#nodeGrad)" stroke="rgba(255,255,255,0.1)"/>
        <text x="90" y="18" text-anchor="middle" fill="#94a3b8" font-size="8" font-weight="600">BACKGROUND SERVICES</text>
        <text x="90" y="40" text-anchor="middle" fill="#818cf8" font-size="9" font-weight="600">minty-search</text>
        <text x="90" y="52" text-anchor="middle" fill="#64748b" font-size="8">@minty-search &#x2022; Slack search</text>
        <text x="90" y="64" text-anchor="middle" fill="#64748b" font-size="8">Multi-query RRF &#x2022; Agentic</text>
        <text x="90" y="84" text-anchor="middle" fill="#818cf8" font-size="9" font-weight="600">slack-index</text>
        <text x="90" y="96" text-anchor="middle" fill="#64748b" font-size="8">Every 4h &#x2192; ChromaDB</text>
        <text x="90" y="116" text-anchor="middle" fill="#818cf8" font-size="9" font-weight="600">workspace-index</text>
        <text x="90" y="128" text-anchor="middle" fill="#64748b" font-size="8">Every 6h &#x2192; ChromaDB</text>
        <text x="90" y="148" text-anchor="middle" fill="#818cf8" font-size="9" font-weight="600">rclone-sync</text>
        <text x="90" y="160" text-anchor="middle" fill="#64748b" font-size="8">Drive sync + backups</text>
        <text x="90" y="180" text-anchor="middle" fill="#818cf8" font-size="9" font-weight="600">cli-health-probe</text>
      </g>

      <!-- ===== Animated dots ===== -->
      <circle cx="88" cy="39" r="3" fill="#e2e8f0" filter="url(#glow)" opacity="0">
        <animate attributeName="cx" dur="12s" repeatCount="indefinite" calcMode="linear"
          keyTimes="0;0.01;0.05;0.09;0.12;0.16;0.19;0.22;0.25;0.28;0.32;0.34;0.38;0.41;0.43;0.47;0.49;0.50;0.51;0.57;0.62;0.68;0.76;0.84;0.93;0.97;1"
          values="88;88;243;379;489;617;617;510;510;580;723;723;723;633;605;605;580;510;510;510;510;510;510;510;510;510;88"/>
        <animate attributeName="cy" dur="12s" repeatCount="indefinite" calcMode="linear"
          keyTimes="0;0.01;0.05;0.09;0.12;0.16;0.19;0.22;0.25;0.28;0.32;0.34;0.38;0.41;0.43;0.47;0.49;0.50;0.51;0.57;0.62;0.68;0.76;0.84;0.93;0.97;1"
          values="39;39;39;39;39;39;85;85;137;130;133;158;237;245;245;152;152;152;170;253;325;398;499;584;584;584;39"/>
        <animate attributeName="r" dur="12s" repeatCount="indefinite" calcMode="linear"
          keyTimes="0;0.01;0.16;0.24;0.25;0.28;0.37;0.38;0.41;0.51;0.62;0.76;0.84;1"
          values="3;3;3;3;5;4.5;4.5;5;4.5;4;3.5;3;3;3"/>
        <animate attributeName="opacity" dur="12s" repeatCount="indefinite" calcMode="linear"
          keyTimes="0;0.01;0.02;0.93;0.97;1"
          values="0;0;0.9;0.9;0;0"/>
      </circle>

      <circle cx="100" cy="129" r="3.5" fill="#f59e0b" opacity="0">
        <animate attributeName="cx" dur="12s" repeatCount="indefinite" calcMode="linear"
          keyTimes="0;0.09;0.10;0.14;0.18;0.22;0.25;1"
          values="100;100;100;268;333;440;510;510"/>
        <animate attributeName="cy" dur="12s" repeatCount="indefinite" calcMode="linear"
          keyTimes="0;0.09;0.10;0.14;0.18;0.22;0.25;1"
          values="129;129;129;129;129;129;137;137"/>
        <animate attributeName="opacity" dur="12s" repeatCount="indefinite" calcMode="linear"
          keyTimes="0;0.09;0.10;0.14;0.20;0.24;0.25;1"
          values="0;0;0.9;0.9;0.9;0.4;0;0"/>
      </circle>

      <circle cx="896" cy="237" r="3" fill="#fbbf24" opacity="0">
        <animate attributeName="cx" dur="12s" repeatCount="indefinite" calcMode="linear"
          keyTimes="0;0.28;0.29;0.32;0.35;0.37;0.38;1"
          values="896;896;896;846;813;750;723;723"/>
        <animate attributeName="opacity" dur="12s" repeatCount="indefinite" calcMode="linear"
          keyTimes="0;0.28;0.29;0.32;0.35;0.37;0.38;1"
          values="0;0;0.9;0.9;0.9;0.4;0;0"/>
      </circle>
    </svg>
  </div>

  <div class="pipeline-mobile">
    <svg viewBox="0 0 450 750" width="100%" xmlns="http://www.w3.org/2000/svg" style="font-family:'JetBrains Mono',monospace">
      <defs>
        <marker id="mArrCyan" markerWidth="7" markerHeight="5" refX="7" refY="2.5" orient="auto"><path d="M0,0 L7,2.5 L0,5" fill="#22d3ee"/></marker>
        <marker id="mArrGreen" markerWidth="7" markerHeight="5" refX="7" refY="2.5" orient="auto"><path d="M0,0 L7,2.5 L0,5" fill="#2ec4b6"/></marker>
        <marker id="mArrAmber" markerWidth="7" markerHeight="5" refX="7" refY="2.5" orient="auto"><path d="M0,0 L7,2.5 L0,5" fill="#fbbf24"/></marker>
        <marker id="mArrOrange" markerWidth="7" markerHeight="5" refX="7" refY="2.5" orient="auto"><path d="M0,0 L7,2.5 L0,5" fill="#f59e0b"/></marker>
        <marker id="mArrPink" markerWidth="7" markerHeight="5" refX="7" refY="2.5" orient="auto"><path d="M0,0 L7,2.5 L0,5" fill="#f472b6"/></marker>
        <marker id="mArrPurple" markerWidth="7" markerHeight="5" refX="7" refY="2.5" orient="auto"><path d="M0,0 L7,2.5 L0,5" fill="#a78bfa"/></marker>
        <marker id="mArrTeal" markerWidth="7" markerHeight="5" refX="7" refY="2.5" orient="auto"><path d="M0,0 L7,2.5 L0,5" fill="#2dd4bf"/></marker>
        <marker id="mArrBlue" markerWidth="7" markerHeight="5" refX="7" refY="2.5" orient="auto"><path d="M0,0 L7,2.5 L0,5" fill="#22d3ee"/></marker>
        <linearGradient id="mGrad" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stop-color="#1a1f2c"/><stop offset="100%" stop-color="#13171f"/></linearGradient>
        <filter id="mGlow"><feGaussianBlur stdDeviation="3" result="b"/><feFlood flood-color="#2ec4b6" flood-opacity="0.3" result="c"/><feComposite in="c" in2="b" operator="in" result="cb"/><feMerge><feMergeNode in="cb"/><feMergeNode in="SourceGraphic"/></feMerge></filter>
      </defs>

      <!-- ROW 0 -->
      <g class="pn" data-tip="Six input sources: Twitter/X, arXiv preprints, RSS feeds, Substack newsletters, Bluesky, and email newsletters." style="cursor:pointer" transform="translate(8,10)">
        <rect width="112" height="38" fill="url(#mGrad)" stroke="rgba(255,255,255,0.1)"/>
        <text x="56" y="16" text-anchor="middle" fill="#94a3b8" font-size="8" font-weight="600">SOURCES</text>
        <text x="56" y="28" text-anchor="middle" fill="#e2e8f0" font-size="6.5">Twitter, arXiv, RSS, Bsky, Email</text>
      </g>
      <line x1="120" y1="29" x2="133" y2="29" stroke="#22d3ee" stroke-width="1.2" marker-end="url(#mArrCyan)"/>
      <g class="pn" data-tip="Fetches from all sources, deduplicates, then scores each item with Claude Opus across six weighted dimensions." style="cursor:pointer" transform="translate(141,10)">
        <rect width="108" height="38" fill="url(#mGrad)" stroke="#22d3ee" stroke-width="1.2"/>
        <text x="54" y="16" text-anchor="middle" fill="#22d3ee" font-size="9" font-weight="700">feedme-daily</text>
        <text x="54" y="28" text-anchor="middle" fill="#94a3b8" font-size="7">Classify, Summarise, Rank</text>
      </g>
      <line x1="249" y1="29" x2="267" y2="29" stroke="#22d3ee" stroke-width="1.2" marker-end="url(#mArrCyan)"/>
      <g class="pn" data-tip="Daily Slack channels where classified items land. Seth reviews and thumbs-up the ones worth promoting." style="cursor:pointer" transform="translate(275,10)">
        <rect width="90" height="38" fill="#0f1520" stroke="rgba(255,255,255,0.1)"/>
        <text x="45" y="16" text-anchor="middle" fill="#fbbf24" font-size="9" font-weight="700">#feed-{'{'}day{'}'}</text>
        <text x="45" y="28" text-anchor="middle" fill="#94a3b8" font-size="7">Slack channel</text>
      </g>

      <!-- ROW 1 -->
      <line x1="320" y1="48" x2="320" y2="70" stroke="#fbbf24" stroke-width="1" stroke-dasharray="3,2" marker-end="url(#mArrAmber)"/>
      <g class="pn" data-tip="Human-in-the-loop curation. The only manual step in the pipeline." style="cursor:pointer" transform="translate(290,76)">
        <rect width="60" height="30" fill="url(#mGrad)" stroke="#fbbf24" stroke-dasharray="4,3"/>
        <text x="30" y="20" text-anchor="middle" fill="#fbbf24" font-size="9">Seth &#x1F44D;</text>
      </g>

      <!-- ROW 2 -->
      <line x1="320" y1="106" x2="320" y2="128" stroke="#2ec4b6" stroke-width="1.2" marker-end="url(#mArrGreen)"/>
      <g class="pn" data-tip="Watches #feed channels for thumbs-up reactions every 5 minutes. Copies promoted messages to #firehose with PDF attachments." style="cursor:pointer" transform="translate(260,134)">
        <rect width="120" height="34" fill="url(#mGrad)" stroke="#2ec4b6" stroke-width="1.2"/>
        <text x="60" y="14" text-anchor="middle" fill="#2ec4b6" font-size="9" font-weight="700">feed-promote</text>
        <text x="60" y="26" text-anchor="middle" fill="#94a3b8" font-size="7">Copy to #firehose</text>
      </g>

      <line x1="320" y1="168" x2="320" y2="195" stroke="#2ec4b6" stroke-width="1.2" marker-end="url(#mArrGreen)"/>

      <!-- Devices spandrel -->
      <g class="pn" data-tip="Direct sharing from iPhone share sheet, Mac hotkey, Chrome extension, or manual paste." style="cursor:pointer" transform="translate(8,100)">
        <rect width="78" height="32" fill="url(#mGrad)" stroke="#fbbf24" stroke-dasharray="4,3"/>
        <text x="39" y="13" text-anchor="middle" fill="#fbbf24" font-size="8" font-weight="600">DEVICES</text>
        <text x="39" y="25" text-anchor="middle" fill="#e2e8f0" font-size="6">iPhone, Mac, Chrome</text>
      </g>
      <line x1="86" y1="116" x2="100" y2="116" stroke="#f59e0b" stroke-width="1.2" marker-end="url(#mArrOrange)"/>
      <g class="pn" data-tip="Real-time intake via Slack Socket Mode. Classifies, enriches, and posts to #firehose immediately." style="cursor:pointer" transform="translate(108,100)">
        <rect width="100" height="32" fill="url(#mGrad)" stroke="#f59e0b" stroke-width="1.2"/>
        <text x="50" y="13" text-anchor="middle" fill="#f59e0b" font-size="9" font-weight="700">share-to-minty</text>
        <text x="50" y="25" text-anchor="middle" fill="#94a3b8" font-size="7">Classify &amp; post</text>
      </g>
      <line x1="158" y1="132" x2="158" y2="210" stroke="#f59e0b" stroke-width="1.2"/>
      <line x1="158" y1="210" x2="243" y2="210" stroke="#f59e0b" stroke-width="1.2" marker-end="url(#mArrOrange)"/>

      <!-- #firehose hub -->
      <g class="pn" data-tip="The central hub channel. All curated content converges here. Three downstream daemons read from it." style="cursor:pointer" transform="translate(250,200)">
        <rect width="155" height="65" fill="#0f1520" stroke="#22d3ee" stroke-width="2" filter="url(#mGlow)"/>
        <text x="78" y="22" text-anchor="middle" fill="#22d3ee" font-size="13" font-weight="800">#firehose</text>
        <text x="78" y="38" text-anchor="middle" fill="#94a3b8" font-size="8">Automated Hub</text>
        <text x="78" y="52" text-anchor="middle" fill="#64748b" font-size="7">2 writers / 3 readers</text>
      </g>

      <!-- Left branch: digest loop -->
      <line x1="250" y1="245" x2="155" y2="245" stroke="#f472b6" stroke-width="1.2" marker-end="url(#mArrPink)"/>

      <g transform="translate(40,180)">
        <rect width="48" height="20" fill="url(#mGrad)" stroke="rgba(255,255,255,0.1)"/>
        <text x="24" y="14" text-anchor="middle" fill="#f472b6" font-size="7" font-weight="600">Email</text>
      </g>
      <line x1="64" y1="225" x2="64" y2="207" stroke="#f472b6" stroke-width="1" marker-end="url(#mArrPink)"/>

      <g class="pn" data-tip="Generates a narrative daily digest using Claude, sends as rich HTML email and posts to #mint-community." style="cursor:pointer" transform="translate(10,225)">
        <rect width="140" height="40" fill="url(#mGrad)" stroke="#f472b6" stroke-width="1.2"/>
        <text x="70" y="16" text-anchor="middle" fill="#f472b6" font-size="9" font-weight="700">yesterday-in-ai</text>
        <text x="70" y="28" text-anchor="middle" fill="#94a3b8" font-size="7">Daily/weekly digest</text>
      </g>

      <line x1="80" y1="265" x2="80" y2="295" stroke="#f472b6" stroke-width="1.2" marker-end="url(#mArrPink)"/>
      <text x="98" y="287" fill="#f472b6" font-size="6" opacity=".8" font-style="italic">digest</text>

      <g class="pn" data-tip="The lab's discussion channel. Receives the daily digest. Lab members discuss and share reactions." style="cursor:pointer" transform="translate(10,303)">
        <rect width="140" height="36" fill="#0f1520" stroke="#2ec4b6" stroke-width="1.2"/>
        <text x="70" y="14" text-anchor="middle" fill="#2ec4b6" font-size="9" font-weight="700">#mint-community</text>
        <text x="70" y="26" text-anchor="middle" fill="#94a3b8" font-size="7">Lab discussion</text>
      </g>

      <line x1="80" y1="339" x2="80" y2="352" stroke="#fbbf24" stroke-width="0.8" stroke-dasharray="3,2"/>
      <g transform="translate(38,354)">
        <rect width="84" height="22" fill="url(#mGrad)" stroke="#fbbf24" stroke-dasharray="3,2"/>
        <text x="42" y="15" text-anchor="middle" fill="#fbbf24" font-size="7">Lab Q&amp;A</text>
      </g>

      <line x1="150" y1="321" x2="270" y2="321" stroke="#2ec4b6" stroke-width="1.2"/>
      <line x1="270" y1="321" x2="270" y2="272" stroke="#2ec4b6" stroke-width="1.2" marker-end="url(#mArrGreen)"/>
      <text x="220" y="336" fill="#2ec4b6" font-size="6" opacity=".7" font-style="italic">loops back</text>

      <text x="80" y="390" text-anchor="middle" fill="#f59e0b" font-size="6" opacity=".6" font-style="italic">share-to-minty also watches</text>

      <!-- Right branch: paper pipeline -->
      <line x1="328" y1="265" x2="328" y2="390" stroke="#a78bfa" stroke-width="1.2" marker-end="url(#mArrPurple)"/>

      <g class="pn" data-tip="Polls #firehose for URLs without PDFs. Three-tier strategy: pattern matching, thread expansion, then sandboxed Claude agent fallback." style="cursor:pointer" transform="translate(263,398)">
        <rect width="130" height="40" fill="url(#mGrad)" stroke="#a78bfa" stroke-width="1.2"/>
        <text x="65" y="16" text-anchor="middle" fill="#a78bfa" font-size="9" font-weight="700">news-pdf-bot</text>
        <text x="65" y="28" text-anchor="middle" fill="#94a3b8" font-size="7">3-tier PDF download</text>
      </g>
      <line x1="328" y1="438" x2="328" y2="465" stroke="#a78bfa" stroke-width="1.2" marker-end="url(#mArrPurple)"/>

      <g class="pn" data-tip="Slack channel where downloaded PDFs and metadata are posted as thread replies." style="cursor:pointer" transform="translate(273,473)">
        <rect width="110" height="32" fill="#0f1520" stroke="#fbbf24" stroke-width="0.8"/>
        <text x="55" y="13" text-anchor="middle" fill="#fbbf24" font-size="9" font-weight="700">#papers</text>
        <text x="55" y="25" text-anchor="middle" fill="#94a3b8" font-size="7">PDFs + notifications</text>
      </g>
      <line x1="328" y1="505" x2="328" y2="532" stroke="#2dd4bf" stroke-width="1.2" marker-end="url(#mArrTeal)"/>

      <g class="pn" data-tip="11-stage pipeline: download, rename, markdown conversion, embed, enrich, analyze (41 questions), RAPTOR summaries, verify, Drive upload, Zotero, report." style="cursor:pointer" transform="translate(263,540)">
        <rect width="130" height="40" fill="url(#mGrad)" stroke="#2dd4bf" stroke-width="1.2"/>
        <text x="65" y="16" text-anchor="middle" fill="#2dd4bf" font-size="9" font-weight="700">corpus-ingest</text>
        <text x="65" y="28" text-anchor="middle" fill="#94a3b8" font-size="7">11-stage pipeline</text>
      </g>
      <line x1="328" y1="580" x2="328" y2="607" stroke="#2dd4bf" stroke-width="1.2" marker-end="url(#mArrTeal)"/>

      <g class="pn" data-tip="Vector database with ~2,200 papers, 48 embedding dimensions, 154K text chunks, 29 topic clusters, and 11,570 pre-computed similarity pairs." style="cursor:pointer" transform="translate(258,615)">
        <rect width="140" height="42" fill="url(#mGrad)" stroke="#2dd4bf" stroke-width="1.2"/>
        <text x="70" y="15" text-anchor="middle" fill="#2dd4bf" font-size="9" font-weight="700">LanceDB Corpus</text>
        <text x="70" y="27" text-anchor="middle" fill="#94a3b8" font-size="7">~2,200 papers</text>
        <text x="70" y="37" text-anchor="middle" fill="#64748b" font-size="6">41 semantic dimensions</text>
      </g>
      <line x1="328" y1="657" x2="328" y2="684" stroke="#22d3ee" stroke-width="1.2" marker-end="url(#mArrBlue)"/>

      <g class="pn" data-tip="Slack bot (@Minty) with 7 search modes including fast search, deep review, catch me up, and unified multi-modal search." style="cursor:pointer" transform="translate(248,692)">
        <rect width="160" height="42" fill="url(#mGrad)" stroke="#22d3ee" stroke-width="1.2"/>
        <text x="80" y="15" text-anchor="middle" fill="#22d3ee" font-size="9" font-weight="700">corpus-agent</text>
        <text x="80" y="27" text-anchor="middle" fill="#94a3b8" font-size="7">@Minty &bull; 7 search modes</text>
        <text x="80" y="37" text-anchor="middle" fill="#64748b" font-size="6">Deep Review &bull; Catch Me Up</text>
      </g>

      <!-- Background services -->
      <g transform="translate(10,450)">
        <rect width="170" height="150" fill="url(#mGrad)" stroke="rgba(255,255,255,0.1)"/>
        <text x="85" y="14" text-anchor="middle" fill="#94a3b8" font-size="7" font-weight="600">BACKGROUND SERVICES</text>
        <text x="85" y="36" text-anchor="middle" fill="#818cf8" font-size="8" font-weight="600">minty-search</text>
        <text x="85" y="48" text-anchor="middle" fill="#64748b" font-size="6">Slack semantic search</text>
        <text x="85" y="68" text-anchor="middle" fill="#818cf8" font-size="8" font-weight="600">slack-index</text>
        <text x="85" y="80" text-anchor="middle" fill="#64748b" font-size="6">Every 4h &#x2192; ChromaDB</text>
        <text x="85" y="100" text-anchor="middle" fill="#818cf8" font-size="8" font-weight="600">workspace-index</text>
        <text x="85" y="112" text-anchor="middle" fill="#64748b" font-size="6">Every 6h &#x2192; ChromaDB</text>
        <text x="85" y="132" text-anchor="middle" fill="#818cf8" font-size="8" font-weight="600">rclone-sync</text>
        <text x="85" y="144" text-anchor="middle" fill="#64748b" font-size="6">Drive backups</text>
      </g>

      <!-- Mobile animated dot -->
      <circle cx="65" cy="29" r="2.5" fill="#e2e8f0" filter="url(#mGlow)" opacity="0">
        <animate attributeName="cx" dur="16s" repeatCount="indefinite" calcMode="linear"
          keyTimes="0;0.01;0.04;0.07;0.09;0.11;0.13;0.16;0.20;0.24;0.28;0.32;0.36;0.39;0.43;0.48;0.53;0.58;0.63;0.68;0.73;0.88;0.95;1"
          values="65;65;195;320;320;320;320;250;80;80;80;150;270;270;328;328;328;328;328;328;328;328;328;65"/>
        <animate attributeName="cy" dur="16s" repeatCount="indefinite" calcMode="linear"
          keyTimes="0;0.01;0.04;0.07;0.09;0.11;0.13;0.16;0.20;0.24;0.28;0.32;0.36;0.39;0.43;0.48;0.53;0.58;0.63;0.68;0.73;0.88;0.95;1"
          values="29;29;29;29;91;151;200;245;245;265;321;321;321;265;265;418;489;560;636;713;713;713;713;29"/>
        <animate attributeName="opacity" dur="16s" repeatCount="indefinite" calcMode="linear"
          keyTimes="0;0.01;0.02;0.88;0.95;1"
          values="0;0;0.9;0.9;0;0"/>
      </circle>

      <circle cx="47" cy="116" r="2.5" fill="#f59e0b" opacity="0">
        <animate attributeName="cx" dur="16s" repeatCount="indefinite" calcMode="linear"
          keyTimes="0;0.07;0.08;0.10;0.12;0.14;0.16;1"
          values="47;47;47;158;158;158;250;250"/>
        <animate attributeName="cy" dur="16s" repeatCount="indefinite" calcMode="linear"
          keyTimes="0;0.07;0.08;0.10;0.12;0.14;0.16;1"
          values="116;116;116;116;132;210;210;210"/>
        <animate attributeName="opacity" dur="16s" repeatCount="indefinite" calcMode="linear"
          keyTimes="0;0.07;0.08;0.10;0.14;0.15;0.16;1"
          values="0;0;0.9;0.9;0.9;0.4;0;0"/>
      </circle>
    </svg>
  </div>

  <div class="pipeline-tip" id="pipelineTip"></div>

  <p style="margin-top:20px;color:var(--text-2);font-size:13px"><em>Tap or hover any node in the diagram to see what it does.</em></p>

</section>

<!-- ============ OVERVIEW ============ -->
<Section id="what-is-this" num="01">
  <h2 class="cli-call" data-section-name="What Is This System?"><span class="section-label">What Is This System?</span><span class="cli-line"><span class="fn">Read</span><span class="op">(</span><span class="str">"what_is_this_system"</span><span class="op">)</span><span class="cli-cursor"></span></span></h2>
  <div class="line-numbered">
  <p class="lead">A research automation platform built around <strong>Minty</strong>, an AI research management agent running on a dedicated Mac Studio. 17 background daemons <span class="t-cyan">fetch</span>, <span class="t-green">classify</span>, <span class="t-yellow">ingest</span>, <span class="t-purple">index</span>, <span class="t-pink">summarize</span>, and <span class="t-blue">distribute</span> academic content &mdash; plus enable deep multi-agent corpus analysis. Research superpowers, not a replacement for researchers.</p>

  <p>External sources (<span class="t-cyan">Twitter</span>, <span class="t-green">arXiv</span>, <span class="t-yellow">RSS</span>, <span class="t-purple">Substack</span>, <span class="t-pink">Bluesky</span>, <span class="t-blue">email</span>) are fetched, classified by Claude Opus, and posted to Slack. Papers are <span class="t-cyan">downloaded</span>, <span class="t-green">converted to markdown</span>, <span class="t-yellow">embedded</span> in a vector database, <span class="t-purple">analyzed</span> across 41 semantic dimensions, and made <span class="t-pink">searchable</span> via a Slack bot. A daily digest reaches 29 lab members across 11 institutions.</p>
  </div>
</Section>

<!-- ============ CONTENT PIPELINE ============ -->
<Section id="content-pipeline" num="02">
  <h2 class="cli-call" data-section-name="The Content Pipeline"><span class="section-label">The Content Pipeline</span><span class="cli-line"><span class="fn">Agent</span><span class="op">(</span><span class="str">"trace the content pipeline"</span><span class="op">,</span> <span class="kw">subagent_type</span><span class="op">=</span><span class="str">"Explore"</span><span class="op">)</span><span class="cli-cursor"></span></span></h2>
  <p class="lead">All content flows through <code>#firehose</code> &mdash; two daemons write to it, three read from it. Source: <a href="https://github.com/mint-philosophy/minty-private">minty-private</a> under <code>daemons/pipeline/</code>.</p>

  <!-- Flow summary: 7-stage pipeline overview -->
  <div style="margin:28px 0 24px">
    <div class="pipeline-flow">
      <div class="pf-card" style="border-left:3px solid var(--cyan);cursor:pointer"
           data-ingest-tip="6 fetchers pull from Twitter, arXiv, RSS/Substack, Bluesky, Email, and LinkedIn in parallel via a thread pool. A PhilPapers scraper is also available for periodic runs."
           data-ingest-color="#22d3ee">
        <div style="font-size:12px;font-weight:700;color:var(--cyan)">Sources</div>
        <div style="font-size:10px;color:var(--text-3)">6 parallel fetchers</div>
      </div>
      <div class="pf-card" style="border-left:3px solid var(--mint);cursor:pointer"
           data-ingest-tip="Claude Opus scores 6 dimensions (relevance, quality, authority, novelty, actionability, engagement), generates a 5&ndash;10 word headline, assigns one of 16 categories, and explains its reasoning. 16 parallel workers, ~40K tokens per batch."
           data-ingest-color="#2ec4b6">
        <div style="font-size:12px;font-weight:700;color:var(--mint)">Classify</div>
        <div style="font-size:10px;color:var(--text-3)">Opus, 6 dimensions</div>
      </div>
      <div class="pf-card" style="border-left:3px solid var(--amber);cursor:pointer"
           data-ingest-tip="Seth reviews #feed-{day} channels each morning. Thumbs-up promotes items to #firehose via the feed-promote daemon, which polls every 5 minutes."
           data-ingest-color="#f59e0b">
        <div style="font-size:12px;font-weight:700;color:var(--amber)">Curate</div>
        <div style="font-size:10px;color:var(--text-3)">Human review</div>
      </div>
      <div class="pf-card pf-hub" style="border-left:3px solid var(--cyan);cursor:pointer"
           data-ingest-tip="Central hub. Everything curated lands here. Three downstream daemons read from it: news-pdf-bot, corpus-ingest, and yesterday-in-ai."
           data-ingest-color="#22d3ee">
        <div style="font-size:12px;font-weight:700;color:var(--cyan)">#firehose</div>
        <div style="font-size:10px;color:var(--text-3)">Central hub</div>
      </div>
      <div class="pf-card" style="border-left:3px solid var(--purple);cursor:pointer"
           data-ingest-tip="news-pdf-bot attaches PDFs via 3-tier acquisition (pattern matching, thread expansion, sandboxed Claude agent). corpus-ingest runs an 11-stage pipeline: markdown conversion, embedding, metadata extraction, cataloguing."
           data-ingest-color="#a78bfa">
        <div style="font-size:12px;font-weight:700;color:var(--purple)">Ingest</div>
        <div style="font-size:10px;color:var(--text-3)">PDFs + corpus pipeline</div>
      </div>
      <div class="pf-card" style="border-left:3px solid var(--indigo);cursor:pointer"
           data-ingest-tip="corpus-agent performs deep semantic analysis on ingested papers: 41 analytical questions, thematic clustering, cross-paper connection mapping."
           data-ingest-color="#818cf8">
        <div style="font-size:12px;font-weight:700;color:var(--indigo)">Analyse</div>
        <div style="font-size:10px;color:var(--text-3)">41Q deep analysis</div>
      </div>
      <div class="pf-card" style="border-left:3px solid var(--pink);cursor:pointer"
           data-ingest-tip="yesterday-in-ai generates a daily narrative digest for subscribers and posts to #mint-community. A weekly Minty's Week in AI summary is produced for lab meetings."
           data-ingest-color="#f472b6">
        <div style="font-size:12px;font-weight:700;color:var(--pink)">Report</div>
        <div style="font-size:10px;color:var(--text-3)">Daily + weekly digests</div>
      </div>
    </div>
  </div>

  <h3 style="margin-top:28px;font-size:14px;color:var(--cyan)">Intake Path 1: Automated Discovery</h3>

  <p>The <strong>feedme-daily</strong> daemon runs daily, fetching from six source types in parallel via a thread pool:</p>

  <RefTable>
    <thead><tr><th>Source</th><th>Method</th><th>Window</th><th>Max Items</th></tr></thead>
    <tbody>
      <tr><td>Twitter/X</td><td>API v2 (OAuth1)</td><td>24h</td><td>200</td></tr>
      <tr><td>Bluesky</td><td>Feed API</td><td>24h</td><td>200</td></tr>
      <tr><td>RSS/Substack</td><td>Feed parser</td><td>7 days</td><td>30</td></tr>
      <tr><td>arXiv</td><td>API</td><td>5 days</td><td>50</td></tr>
      <tr><td>LinkedIn</td><td>Browser automation</td><td>100 posts</td><td>50</td></tr>
      <tr><td>Email newsletters</td><td>Gmail API</td><td>7 days</td><td>30</td></tr>
    </tbody>
  </RefTable>

  <p><strong>Deduplication</strong> uses a union-find algorithm with multiple keys per item &mdash; content hash, URL hash, and canonical IDs (arXiv, DOI, YouTube). Items sharing any key are grouped; the canonical item is chosen by engagement, then length, then timestamp. Unique items advance to classification.</p>

  <p><strong>Classification</strong> uses Claude Opus with 16 concurrent workers in ~40K-token batches. Each item is scored across six dimensions:</p>

  <RefTable>
    <thead><tr><th>Dimension</th><th>Weight</th><th>What It Measures</th></tr></thead>
    <tbody>
      <tr><td style="color:var(--mint)">Relevance</td><td>0.30</td><td>Topic match to MINT Lab interests &mdash; 30+ specific areas from normative competence to political economy, with explicit scoring guidance per topic</td></tr>
      <tr><td style="color:var(--cyan)">Quality</td><td>0.25</td><td>Substance vs. noise. Length-agnostic: a concise tweet with genuine insight scores as well as a long paper</td></tr>
      <tr><td style="color:var(--indigo)">Authority</td><td>0.15</td><td>Author/institution credibility. Named labs (Anthropic, DeepMind, etc.) score 0.9+</td></tr>
      <tr><td style="color:var(--purple)">Novelty</td><td>0.15</td><td>Breaking news to stale content</td></tr>
      <tr><td style="color:var(--amber)">Actionability</td><td>0.10</td><td>Urgency and time-sensitivity</td></tr>
      <tr><td style="color:var(--text-3)">Engagement</td><td>0.05</td><td>Social proof normalized to platform norms (weak signal)</td></tr>
    </tbody>
  </RefTable>

  <p>Thresholds are source-specific: arXiv &ge;0.60, PhilPapers &ge;0.45, everything else &ge;0.50. Topics <code>progress_studies</code> and <code>ai_science</code> get a +0.06 boost. Allowlisted accounts bypass classification entirely.</p>

  <h3 style="margin-top:28px;font-size:14px;color:var(--mint)">Inside the Classification Prompt</h3>
  <p style="font-size:13px;color:var(--text-2);margin-bottom:16px">Every item is evaluated against a 177-line classification prompt encoding the lab's identity, topic taxonomy, quality heuristics, scoring floors, and hard exclusions. Tap or hover each theme for exact prompt language.</p>

  <div class="cards" style="grid-template-columns: repeat(auto-fill, minmax(200px, 1fr))">
    <div class="card" style="border-color:var(--mint)" data-ingest-tip="Anchors every classification in the lab's institutional identity and research mission. From the prompt: 'You are a research feed classifier for the MINT Research Lab at Johns Hopkins University, directed by Seth Lazar. The lab works on AI safety, normative competence of LLMs, agentic AI governance, and political philosophy of AI.' And: 'The lab's research is situated within a broader concern: liberal democracies face institutional, economic, and cultural decline. AI offers the best opportunity to reverse this trajectory — but only if deployed wisely.'" data-ingest-color="#2ec4b6">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(46,196,182,0.08);color:var(--mint)">~$</div>
        <div><div class="card-title">Lab Identity &amp; Mission</div><div class="card-sub">Framed around institutional renewal</div></div>
      </div>
    </div>
    <div class="card" style="border-color:var(--cyan)" data-ingest-tip="Defines 30+ equally weighted interest areas — the classifier must not treat any as 'core' vs. 'peripheral.' From the prompt: 'ALL of these interests are equally important: normative competence, benchmark methodology, agent governance, calibrated safety, catastrophic risk, AI and democracy, AI policy, agent security, interpretability, AI economics, platform governance, frontier lab developments, progress studies, advancement of science, abundance agenda, institutional dysfunction/reform...' And: 'Domestic and international politics ONLY when about structural/institutional change. Exclude: commentary about individual politicians, horse-race coverage, partisan debates.'" data-ingest-color="#22d3ee">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(34,211,238,0.08);color:var(--cyan)">[]</div>
        <div><div class="card-title">Topic Taxonomy</div><div class="card-sub">30+ equally weighted interest areas</div></div>
      </div>
    </div>
    <div class="card" style="border-color:var(--indigo)" data-ingest-tip="Explicitly counteracts length bias — judges density of insight, not word count. From the prompt: 'Substance can come in ANY length. A concise post with a genuine insight, novel argument, important evidence, or breaking news is high quality regardless of word count.' And: 'A 280-character post making a genuine point is better than a long rehash.' And: 'Do NOT penalize reshares/link-posts if the underlying content is relevant and substantive. Judge what's being pointed to, not just the wrapper.'" data-ingest-color="#818cf8">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(129,140,248,0.08);color:var(--indigo)">><</div>
        <div><div class="card-title">Quality Over Length</div><div class="card-sub">Density of insight, not word count</div></div>
      </div>
    </div>
    <div class="card" style="border-color:var(--amber)" data-ingest-tip="Sets scoring floors for strategically important sources — frontier model releases, trusted authors, and global labs. From the prompt: 'New foundation model releases from ANY lab worldwide — score 0.85+ relevance. This includes video/image/audio models, not just language models.' And: 'Chinese labs (DeepSeek, ByteDance/Doubao, Baidu ERNIE, Alibaba Qwen, Zhipu GLM, Moonshot/Kimi, 01.AI/Yi) — score 0.85+. Track the global frontier, not just Western labs.' Named boost targets include Institute for Progress, Works in Progress, Patrick Collison, Tyler Cowen, and others." data-ingest-color="#f59e0b">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(245,158,11,0.08);color:var(--amber)">^^</div>
        <div><div class="card-title">Boosted Sources</div><div class="card-sub">Scoring floors for key sources</div></div>
      </div>
    </div>
    <div class="card" style="border-color:var(--purple)" data-ingest-tip="Specialised handling for philosophy papers — topic relevance and abstract quality trump journal prestige. From the prompt: 'Seth is interested in philosophy of AI broadly — he wants to see what's being published in this space, even from lesser-known journals and authors.' And: 'Judge primarily on TOPIC RELEVANCE and ABSTRACT QUALITY, not journal prestige.' AI consciousness, model welfare, and AI personhood all score 0.8+ relevance." data-ingest-color="#a78bfa">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(167,139,250,0.08);color:var(--purple)">&phi;</div>
        <div><div class="card-title">PhilPapers</div><div class="card-sub">Topic relevance over journal prestige</div></div>
      </div>
    </div>
    <div class="card" style="border-color:var(--pink)" data-ingest-tip="Explicit exclusion list that prevents noise from reaching the team. From the prompt: 'anti_interest: hype, crypto, celebrity gossip, partisan political commentary, culture war, ethics-slop, marketing emails, promotional content, webinar invitations.' And: 'Ethics-slop — vague normative language without testable claims.' Marketing content scores Relevance 0.1, Quality 0.1 regardless of topic." data-ingest-color="#f472b6">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(244,114,182,0.08);color:var(--pink)">--</div>
        <div><div class="card-title">Hard Exclusions</div><div class="card-sub">Crypto, hype, ethics-slop, marketing</div></div>
      </div>
    </div>
    <div class="card" style="border-color:var(--green)" data-ingest-tip="Detects academic paper links for downstream enrichment — PDF download, abstract extraction, corpus ingestion. From the prompt: 'Check whether the content references or links to academic papers/preprints. Look for: arXiv URLs, DOI links, academic publisher URLs (nature.com, science.org, pnas.org, openreview.net, aclweb.org, etc.).' Detected paper URLs are passed to the enrichment stage." data-ingest-color="#4ade80">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(74,222,128,0.08);color:var(--green)">&sect;</div>
        <div><div class="card-title">Paper Detection</div><div class="card-sub">Academic papers flagged for enrichment</div></div>
      </div>
    </div>
    <div class="card" style="border-color:#8892a6" data-ingest-tip="The prompt's default posture: when uncertain, let it through for human review rather than filtering it out. From the prompt: 'When in doubt about relevance, err toward higher scores — the lab team reviews after.' Target pass rate is 25&ndash;30%. 'Do NOT shoehorn items into categories where they don't clearly belong.'" data-ingest-color="#8892a6">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(136,146,166,0.08);color:#8892a6">[+]</div>
        <div><div class="card-title">Err Toward Inclusion</div><div class="card-sub">25&ndash;30% target pass rate</div></div>
      </div>
    </div>
  </div>

  <h3 style="margin-top:28px;font-size:14px;color:var(--mint)">After Classification: Upsampling &amp; Delivery</h3>
  <p style="font-size:13px;color:var(--text-2);margin-bottom:16px">Classification also transforms each item into a structured record. Four further stages prepare items for delivery.</p>

  <div style="display:grid;grid-template-columns:3px 1fr;gap:0 16px;margin:20px 0">
    <div style="background:var(--mint);border-radius:0"></div>
    <div style="padding:12px 0">
      <div style="font-size:13px;font-weight:700;color:var(--mint);margin-bottom:4px">Upsampling</div>
      <div style="font-size:12.5px;color:var(--text-2);line-height:1.6">Generates a <strong>5&ndash;10 word headline</strong>, assigns one of <strong>16 categories</strong>, writes a <strong>classification reason</strong>, and flags <strong>paper links</strong>. Every item emerges with six scores, a category, a headline, and a reason.</div>
    </div>
    <div style="background:var(--cyan);border-radius:0"></div>
    <div style="padding:12px 0;border-top:1px solid var(--border)">
      <div style="font-size:13px;font-weight:700;color:var(--cyan);margin-bottom:4px">Summarisation</div>
      <div style="font-size:12.5px;color:var(--text-2);line-height:1.6">Parallel Opus pass for long-form content (RSS, Substack, email, posts &gt;3K chars). Generates an intelligent reading &mdash; central insight first, supporting arguments with data, original word count. Honest about partial sources.</div>
    </div>
    <div style="background:var(--amber);border-radius:0"></div>
    <div style="padding:12px 0;border-top:1px solid var(--border)">
      <div style="font-size:13px;font-weight:700;color:var(--amber);margin-bottom:4px">Enrichment</div>
      <div style="font-size:12.5px;color:var(--text-2);line-height:1.6">Resolves shortlinks, scrapes <strong>abstracts</strong> from 30+ publisher domains, and downloads <strong>PDFs</strong> via publisher-specific URL transforms and the ANU institutional proxy.</div>
    </div>
    <div style="background:var(--indigo);border-radius:0"></div>
    <div style="padding:12px 0;border-top:1px solid var(--border)">
      <div style="font-size:13px;font-weight:700;color:var(--indigo);margin-bottom:4px">Posting to Slack</div>
      <div style="font-size:12.5px;color:var(--text-2);line-height:1.6">Sorted by composite score and posted to daily <code style="font-size:11px">#feed-{'{'}dayname{'}'}</code> channels. Headline in bold, inline content or summary depending on length. Metadata footer with score, category, and reason. Abstracts and PDFs as thread replies.</div>
    </div>
    <div style="background:var(--pink);border-radius:0"></div>
    <div style="padding:12px 0;border-top:1px solid var(--border)">
      <div style="font-size:13px;font-weight:700;color:var(--pink);margin-bottom:4px">Promotion</div>
      <div style="font-size:12.5px;color:var(--text-2);line-height:1.6">Seth reviews <code style="font-size:11px">#feed</code> each morning and reacts with &#x1F44D;. The <strong>feed-promote</strong> daemon copies promoted messages to <code style="font-size:11px">#firehose</code> (including PDF attachments) and tracks IDs to prevent duplicates.</div>
    </div>
  </div>

  <h3 style="margin-top:24px;font-size:14px;color:var(--amber)">Intake Path 2: Manual Sharing</h3>

  <p>The <strong>share-to-minty</strong> daemon provides real-time intake via Slack Socket Mode. URLs shared from any device (iPhone, Mac hotkey, Chrome extension, or <code>#minty-inbox</code>) are immediately classified, enriched, and posted to <code>#firehose</code> &mdash; bypassing the daily batch cycle.</p>

  <h3 style="margin-top:24px;font-size:14px;color:var(--pink)">Reader 1: yesterday-in-ai</h3>

  <p>Generates a narrative daily digest from yesterday's <code>#firehose</code> posts &mdash; intelligent prose, not a list. Sent as HTML email to 29 lab members across 11 institutions, posted to <code>#mint-community</code>, and published to Ghost. A weekly summary also ships before lab meetings.</p>

  <h3 style="margin-top:24px;font-size:14px;color:var(--purple)">Reader 2: news-pdf-bot</h3>

  <p style="font-size:13px;color:var(--text-2);margin-bottom:16px">Polls <code>#firehose</code> for messages with links lacking an attached PDF. Three-tier acquisition, escalating until found.</p>

  <div style="display:grid;grid-template-columns:3px 1fr;gap:0 16px;margin:20px 0">
    <div style="background:var(--purple);border-radius:0"></div>
    <div style="padding:12px 0">
      <div style="font-size:13px;font-weight:700;color:var(--purple);margin-bottom:4px">Tier 1 &mdash; Programmatic Pattern Matching</div>
      <div style="font-size:12.5px;color:var(--text-2);line-height:1.6">Resolves shortlinks and matches against known academic URL patterns (arXiv, OpenReview, bioRxiv, 30+ publishers). Downloads and validates via <code style="font-size:11px">%PDF</code> magic bytes.</div>
    </div>
    <div style="background:var(--purple);border-radius:0"></div>
    <div style="padding:12px 0;border-top:1px solid var(--border)">
      <div style="font-size:13px;font-weight:700;color:var(--purple);margin-bottom:4px">Tier 2 &mdash; Thread Expansion</div>
      <div style="font-size:12.5px;color:var(--text-2);line-height:1.6">If the message links to a tweet that looks like a thread (reply count &gt; 0, or contains &#x1F9F5;, &ldquo;thread&rdquo;, or &ldquo;1/&rdquo;), fetches the full self-reply chain via Twitter API and scans all tweets for paper URLs.</div>
    </div>
    <div style="background:var(--purple);border-radius:0"></div>
    <div style="padding:12px 0;border-top:1px solid var(--border)">
      <div style="font-size:13px;font-weight:700;color:var(--purple);margin-bottom:4px">Tier 3 &mdash; Sandboxed Claude Agent</div>
      <div style="font-size:12.5px;color:var(--text-2);line-height:1.6">Sandboxed Claude Opus agent (max 12 turns, 8 Bash calls) with URL resolution, Semantic Scholar, and Unpaywall. For paywalled content: SeleniumBase headless browser with ANU institutional proxy authentication, or OpenClaw browser automation for sites requiring JavaScript rendering.</div>
    </div>
    <div style="background:var(--text-3);border-radius:0"></div>
    <div style="padding:12px 0;border-top:1px solid var(--border)">
      <div style="font-size:13px;font-weight:700;color:var(--text-2);margin-bottom:4px">Delivery</div>
      <div style="font-size:12.5px;color:var(--text-2);line-height:1.6">PDFs uploaded as thread replies with canonical filenames (<code style="font-size:11px">Author et al - Year - Title.pdf</code>) and abstracts where available. Deduplicates by reply history.</div>
    </div>
  </div>

  <h3 style="margin-top:24px;font-size:14px;color:var(--teal)">Reader 3: corpus-ingest</h3>

  <p>Picks up papers from <code>#firehose</code> and runs the 11-stage ingestion pipeline. Detailed in &sect;03 below.</p>

  <h3 style="margin-top:24px;font-size:14px;color:var(--cyan)">The Result</h3>

  <p>Five minutes of morning review replaces hours of manual tracking. Everything worth following is captured, classified, summarized, stored, analyzed, and shared automatically.</p>
</Section>

<!-- ============ CORPUS INGESTION PIPELINE ============ -->
<Section id="corpus" num="03">
  <h2 class="cli-call" data-section-name="Corpus Ingestion"><span class="section-label">Corpus Ingestion</span><span class="cli-line"><span class="fn">Bash</span><span class="op">(</span><span class="str">"python3 corpus_ingest.py --stages=11"</span><span class="op">)</span><span class="cli-cursor"></span></span></h2>
  <p>The MINT Lab's intellectual engine: ~2,200 papers on AI safety, ethics, alignment, governance, and capabilities &mdash; all embedded, analyzed, and searchable.</p>

  <h3>Corpus-Ingest Pipeline Detail</h3>
  <p>Each paper flows through 11 stages in the unified corpus-ingest daemon. The pipeline supports resume from the last completed stage.</p>

  <div class="ingest-pipeline">
    <div class="ingest-stages">

      <div class="ingest-stage">
        <div class="ingest-dot" style="background:#2dd4bf;box-shadow:0 0 6px rgba(45,212,191,0.4)"></div>
        <div class="ingest-line" style="background:#2dd4bf"></div>
        <div class="ingest-box" style="border-color:#2dd4bf" data-ingest-tip="Fetches PDFs from Slack (#firehose, #papers) or the local-PDFs catchup folder. SHA-256 hash deduplication prevents re-downloading papers already in the corpus. Supports arXiv, Semantic Scholar, direct PDF links, and 15+ publisher-specific download strategies." data-ingest-color="#2dd4bf">
          <span class="ingest-num" style="color:#2dd4bf">1</span>
          <div class="ingest-info">
            <div class="ingest-name" style="color:#2dd4bf">Download</div>
            <div class="ingest-desc">Slack + local PDFs</div>
          </div>
        </div>
      </div>

      <div class="ingest-stage">
        <div class="ingest-dot" style="background:#2dd4bf;box-shadow:0 0 6px rgba(45,212,191,0.4)"></div>
        <div class="ingest-line" style="background:#2dd4bf"></div>
        <div class="ingest-box" style="border-color:#2dd4bf" data-ingest-tip="Extracts metadata (title, authors, year) from the PDF and renames the file to a standardised format: Author1_Author2_Year_ShortTitle.pdf. Uses CrossRef and Semantic Scholar APIs for metadata resolution." data-ingest-color="#2dd4bf">
          <span class="ingest-num" style="color:#2dd4bf">2</span>
          <div class="ingest-info">
            <div class="ingest-name" style="color:#2dd4bf">Rename</div>
            <div class="ingest-desc">Metadata extract</div>
          </div>
        </div>
      </div>

      <div class="ingest-stage ingest-stage--highlight">
        <div class="ingest-dot" style="background:#fb923c;box-shadow:0 0 8px rgba(251,146,60,0.5)"></div>
        <div class="ingest-line" style="background:#fb923c"></div>
        <div class="ingest-box" style="border-color:#fb923c;background:rgba(251,146,60,0.04)" data-ingest-tip="Converts PDF to searchable markdown via a 6-tier cascade: Companion (pre-extracted) &rarr; arXiv HTML &rarr; ar5iv &rarr; Marker &rarr; PyMuPDF &rarr; Tesseract OCR. Each tier is tried only if the previous fails. This is the most failure-prone stage — highlighted because conversion quality directly determines downstream embedding and analysis quality." data-ingest-color="#fb923c">
          <span class="ingest-num" style="color:#fb923c">3</span>
          <div class="ingest-info">
            <div class="ingest-name" style="color:#fb923c">Markdown</div>
            <div class="ingest-desc">6-tier cascade</div>
          </div>
        </div>
      </div>

      <div class="ingest-stage">
        <div class="ingest-dot" style="background:#818cf8;box-shadow:0 0 6px rgba(129,140,248,0.4)"></div>
        <div class="ingest-line" style="background:#818cf8"></div>
        <div class="ingest-box" style="border-color:#818cf8" data-ingest-tip="Generates 2048-dimensional vector embeddings via Voyage AI. Each document receives 48 separate embeddings — one per semantic dimension — enabling multi-lens retrieval. Chunks are also embedded separately for passage-level search." data-ingest-color="#818cf8">
          <span class="ingest-num" style="color:#818cf8">4</span>
          <div class="ingest-info">
            <div class="ingest-name" style="color:#818cf8">Embed</div>
            <div class="ingest-desc">Voyage 2048d</div>
          </div>
        </div>
      </div>

      <div class="ingest-stage">
        <div class="ingest-dot" style="background:#2dd4bf;box-shadow:0 0 6px rgba(45,212,191,0.4)"></div>
        <div class="ingest-line" style="background:#2dd4bf"></div>
        <div class="ingest-box" style="border-color:#2dd4bf" data-ingest-tip="Fetches bibliographic metadata from CrossRef and Semantic Scholar: citation counts, DOI, publication venue, abstract, references, and author affiliations. Resolves arXiv IDs to published versions where available." data-ingest-color="#2dd4bf">
          <span class="ingest-num" style="color:#2dd4bf">5</span>
          <div class="ingest-info">
            <div class="ingest-name" style="color:#2dd4bf">Enrich</div>
            <div class="ingest-desc">CrossRef/S2</div>
          </div>
        </div>
      </div>

      <div class="ingest-stage">
        <div class="ingest-dot" style="background:#a78bfa;box-shadow:0 0 6px rgba(167,139,250,0.4)"></div>
        <div class="ingest-line" style="background:#a78bfa"></div>
        <div class="ingest-box" style="border-color:#a78bfa" data-ingest-tip="Deep semantic analysis via Claude Opus across 41 questions spanning research core (thesis, claims, evidence), methodology, AI-specific dimensions, meta-analysis, and advanced categories. Each answer becomes a searchable text field with its own 2048d embedding vector." data-ingest-color="#a78bfa">
          <span class="ingest-num" style="color:#a78bfa">6</span>
          <div class="ingest-info">
            <div class="ingest-name" style="color:#a78bfa">Analyze</div>
            <div class="ingest-desc">41Q via LLM</div>
          </div>
        </div>
      </div>

      <div class="ingest-stage">
        <div class="ingest-dot" style="background:#a78bfa;box-shadow:0 0 6px rgba(167,139,250,0.4)"></div>
        <div class="ingest-line" style="background:#a78bfa"></div>
        <div class="ingest-box" style="border-color:#a78bfa" data-ingest-tip="Generates hierarchical summaries at section and document level using Claude Opus. RAPTOR (Recursive Abstractive Processing for Tree-Organized Retrieval) summaries enable fast corpus-wide search via compact summary embeddings rather than full-text." data-ingest-color="#a78bfa">
          <span class="ingest-num" style="color:#a78bfa">7</span>
          <div class="ingest-info">
            <div class="ingest-name" style="color:#a78bfa">RAPTOR</div>
            <div class="ingest-desc">Summaries</div>
          </div>
        </div>
      </div>

      <div class="ingest-stage">
        <div class="ingest-dot" style="background:#2dd4bf;box-shadow:0 0 6px rgba(45,212,191,0.4)"></div>
        <div class="ingest-line" style="background:#2dd4bf"></div>
        <div class="ingest-box" style="border-color:#2dd4bf" data-ingest-tip="Validates that all database fields are populated correctly: embeddings present, analysis complete, summaries generated, metadata resolved. Papers failing verification are flagged for manual review." data-ingest-color="#2dd4bf">
          <span class="ingest-num" style="color:#2dd4bf">8</span>
          <div class="ingest-info">
            <div class="ingest-name" style="color:#2dd4bf">Verify</div>
            <div class="ingest-desc">DB check</div>
          </div>
        </div>
      </div>

      <div class="ingest-stage">
        <div class="ingest-dot" style="background:#4ade80;box-shadow:0 0 6px rgba(74,222,128,0.4)"></div>
        <div class="ingest-line" style="background:#4ade80"></div>
        <div class="ingest-box" style="border-color:#4ade80" data-ingest-tip="Uploads the original PDF and generated markdown file to the lab's Google Shared Drive via rclone, organised in folders by topic cluster. Provides browser-accessible copies for lab members without local corpus access." data-ingest-color="#4ade80">
          <span class="ingest-num" style="color:#4ade80">9</span>
          <div class="ingest-info">
            <div class="ingest-name" style="color:#4ade80">Drive</div>
            <div class="ingest-desc">Upload PDF+MD</div>
          </div>
        </div>
      </div>

      <div class="ingest-stage">
        <div class="ingest-dot" style="background:#38bdf8;box-shadow:0 0 6px rgba(56,189,248,0.4)"></div>
        <div class="ingest-line" style="background:#38bdf8"></div>
        <div class="ingest-box" style="border-color:#38bdf8" data-ingest-tip="Creates a catalog entry in the lab's Zotero library with full bibliographic metadata, tags, and links to the Drive copies. Enables citation management and BibTeX export for lab members writing papers." data-ingest-color="#38bdf8">
          <span class="ingest-num" style="color:#38bdf8">10</span>
          <div class="ingest-info">
            <div class="ingest-name" style="color:#38bdf8">Zotero</div>
            <div class="ingest-desc">Catalog</div>
          </div>
        </div>
      </div>

      <div class="ingest-stage ingest-stage--last">
        <div class="ingest-dot" style="background:#f472b6;box-shadow:0 0 6px rgba(244,114,182,0.4)"></div>
        <div class="ingest-box" style="border-color:#f472b6" data-ingest-tip="Posts a completion notification to the #corpus-updates Slack channel with paper title, authors, processing statistics, and any warnings from earlier stages. Lab members can react to trigger further actions." data-ingest-color="#f472b6">
          <span class="ingest-num" style="color:#f472b6">11</span>
          <div class="ingest-info">
            <div class="ingest-name" style="color:#f472b6">Report</div>
            <div class="ingest-desc">Slack notify</div>
          </div>
        </div>
      </div>

    </div>

    <!-- Supplementary info -->
    <div class="ingest-details">
      <div class="ingest-detail-block" style="border-color:#fb923c">
        <div class="ingest-detail-title" style="color:#fb923c">Markdown Conversion Cascade</div>
        <div class="ingest-detail-text">Companion (pre-extracted) &rarr; arXiv HTML &rarr; ar5iv &rarr; Marker &rarr; PyMuPDF &rarr; Tesseract OCR</div>
        <div class="ingest-detail-note">Falls through tiers until successful conversion. Each tier is tried only if the previous one fails.</div>
      </div>
      <div class="ingest-detail-block" style="border-color:#2dd4bf">
        <div class="ingest-detail-title" style="color:#2dd4bf">Worker Pools</div>
        <div class="ingest-detail-text">Pipeline: 8 (local) / 2 (slack)</div>
        <div class="ingest-detail-text">Embed: 4 &bull; Enrich: 4 &bull; Analyze: 6 &bull; RAPTOR: 6</div>
      </div>
    </div>
  </div>

  <div class="grid-2" style="margin-top:16px">
    <div>
      <h3>Scale</h3>
      <RefTable>
        <tbody>
        <tr><td>Papers</td><td><strong>~2,200</strong></td></tr>
        <tr><td>Text chunks</td><td>154,509 (<code>chunks_md</code> table, markdown-aware)</td></tr>
        <tr><td>Semantic analyses</td><td>1,744 (41 questions each)</td></tr>
        <tr><td>RAPTOR summaries</td><td>1,674</td></tr>
        <tr><td>Topic clusters</td><td>29 (with 114 micro-topics)</td></tr>
        <tr><td>Embedding dims per doc</td><td>48 (each 2048d Voyage)</td></tr>
        <tr><td>Total columns</td><td>138 in documents table</td></tr>
        <tr><td>Pre-computed similarities</td><td>11,570 pairs</td></tr>
        <tr><td>LanceDB size</td><td>4.3 GB (compacted after markdown chunk migration)</td></tr>
        </tbody>
      </RefTable>
    </div>
    <div>
      <h3>Search Modes</h3>
      <ul class="clean">
        <li><strong>search-summary</strong> &mdash; RAPTOR summary search (fast, broad)</li>
        <li><strong>search</strong> &mdash; Full-text semantic search (deep)</li>
        <li><strong>search-semantic</strong> &mdash; Argumentative similarity</li>
        <li><strong>search-dimension qNN</strong> &mdash; 41 semantic dimensions</li>
        <li><strong>chunks</strong> &mdash; Passage-level search</li>
        <li><strong>similar / similar-by-dim</strong> &mdash; Pre-computed similarity</li>
        <li><strong>Unified search</strong> &mdash; Multi-modal with RRF fusion</li>
      </ul>
    </div>
  </div>

  <div class="ingest-tip" id="ingestTip"></div>

  <h3>41-Question Semantic Analysis</h3>
  <p>Every paper is analyzed across 41 dimensions, each producing text content and a 2048d embedding vector. Hover any cell to see the full question.</p>

  <h3 style="font-size:12px;color:var(--mint);margin-top:24px">Research Core</h3>
  <div class="q-grid">
    <div class="q-cell" style="border-top-color:var(--mint)"><div class="q-id">q01</div><div class="q-key">research_question</div><div class="q-tooltip">Core research question in one specific sentence</div></div>
    <div class="q-cell" style="border-top-color:var(--mint)"><div class="q-id">q02</div><div class="q-key">thesis</div><div class="q-tooltip">Main thesis or finding in 1&ndash;2 sentences</div></div>
    <div class="q-cell" style="border-top-color:var(--mint)"><div class="q-id">q03</div><div class="q-key">key_claims</div><div class="q-tooltip">3&ndash;5 specific, falsifiable claims</div></div>
    <div class="q-cell" style="border-top-color:var(--mint)"><div class="q-id">q04</div><div class="q-key">evidence</div><div class="q-tooltip">Type and quality of evidence &mdash; experiments, models, sample sizes, rigor</div></div>
    <div class="q-cell" style="border-top-color:var(--mint)"><div class="q-id">q05</div><div class="q-key">limitations</div><div class="q-tooltip">Acknowledged AND unacknowledged limitations</div></div>
  </div>

  <h3 style="font-size:12px;color:var(--cyan);margin-top:16px">Methodology</h3>
  <div class="q-grid">
    <div class="q-cell" style="border-top-color:var(--cyan)"><div class="q-id">q06</div><div class="q-key">paradigm</div><div class="q-tooltip">Research paradigm with detail (not just &lsquo;survey&rsquo;)</div></div>
    <div class="q-cell" style="border-top-color:var(--cyan)"><div class="q-id">q07</div><div class="q-key">methods</div><div class="q-tooltip">Specific methods, techniques, frameworks, tools</div></div>
    <div class="q-cell" style="border-top-color:var(--cyan)"><div class="q-id">q08</div><div class="q-key">data</div><div class="q-tooltip">Datasets, models, sample sizes, versions</div></div>
    <div class="q-cell" style="border-top-color:var(--cyan)"><div class="q-id">q09</div><div class="q-key">reproducibility</div><div class="q-tooltip">Reproducibility assessment -- resources needed, code/data availability</div></div>
    <div class="q-cell" style="border-top-color:var(--cyan)"><div class="q-id">q10</div><div class="q-key">framework</div><div class="q-tooltip">Theoretical/conceptual framework -- specific theories, authors, application</div></div>
  </div>

  <h3 style="font-size:12px;color:var(--indigo);margin-top:16px">Context</h3>
  <div class="q-grid">
    <div class="q-cell" style="border-top-color:var(--indigo)"><div class="q-id">q11</div><div class="q-key">traditions</div><div class="q-tooltip">Intellectual traditions -- academic lineages and schools of thought</div></div>
    <div class="q-cell" style="border-top-color:var(--indigo)"><div class="q-id">q12</div><div class="q-key">key_citations</div><div class="q-tooltip">2-3 papers/authors most engaged with (not just cited)</div></div>
    <div class="q-cell" style="border-top-color:var(--indigo)"><div class="q-id">q13</div><div class="q-key">assumptions</div><div class="q-tooltip">Key assumptions -- philosophical, empirical, scope</div></div>
    <div class="q-cell" style="border-top-color:var(--indigo)"><div class="q-id">q14</div><div class="q-key">counterarguments</div><div class="q-tooltip">Substantive objections from different perspectives</div></div>
  </div>

  <h3 style="font-size:12px;color:var(--purple);margin-top:16px">AI-Specific</h3>
  <div class="q-grid">
    <div class="q-cell" style="border-top-color:var(--purple)"><div class="q-id">q15</div><div class="q-key">llm_role</div><div class="q-tooltip">Category (SUBJECT/TOOL/CONCERN/TECHNICAL/NONE) + detailed explanation</div></div>
    <div class="q-cell" style="border-top-color:var(--purple)"><div class="q-id">q16</div><div class="q-key">models</div><div class="q-tooltip">Specific AI/LLM models discussed -- names, versions, comparisons</div></div>
    <div class="q-cell" style="border-top-color:var(--purple)"><div class="q-id">q17</div><div class="q-key">capability_claims</div><div class="q-tooltip">Claims about what LLMs can or cannot do</div></div>
    <div class="q-cell" style="border-top-color:var(--purple)"><div class="q-id">q18</div><div class="q-key">risk_claims</div><div class="q-tooltip">Claims about risks, dangers, harms -- severity, likelihood, evidence</div></div>
  </div>

  <h3 style="font-size:12px;color:var(--amber);margin-top:16px">Meta</h3>
  <div class="q-grid">
    <div class="q-cell" style="border-top-color:var(--amber)"><div class="q-id">q19</div><div class="q-key">novelty</div><div class="q-tooltip">What is claimed as new -- method, evidence, framework</div></div>
    <div class="q-cell" style="border-top-color:var(--amber)"><div class="q-id">q20</div><div class="q-key">field</div><div class="q-tooltip">Primary subfield or research area</div></div>
    <div class="q-cell" style="border-top-color:var(--amber)"><div class="q-id">q21</div><div class="q-key">audience</div><div class="q-tooltip">Intended readers</div></div>
    <div class="q-cell" style="border-top-color:var(--amber)"><div class="q-id">q22</div><div class="q-key">implications</div><div class="q-tooltip">Practical implications if claims are correct</div></div>
    <div class="q-cell" style="border-top-color:var(--amber)"><div class="q-id">q23</div><div class="q-key">future_work</div><div class="q-tooltip">Suggested future directions -- explicit and implicit</div></div>
    <div class="q-cell" style="border-top-color:var(--amber)"><div class="q-id">q24</div><div class="q-key">quality</div><div class="q-tooltip">Quality rating 1-5 with explanation</div></div>
    <div class="q-cell" style="border-top-color:var(--amber)"><div class="q-id">q25</div><div class="q-key">stance</div><div class="q-tooltip">Stance on AI development -- nuanced characterization</div></div>
    <div class="q-cell" style="border-top-color:var(--amber)"><div class="q-id">q26</div><div class="q-key">other</div><div class="q-tooltip">Anything else notable -- surprising findings, innovations</div></div>
  </div>

  <h3 style="font-size:12px;color:var(--teal);margin-top:16px">Advanced</h3>
  <div class="q-grid">
    <div class="q-cell" style="border-top-color:var(--teal)"><div class="q-id">q27</div><div class="q-key">source_type</div><div class="q-tooltip">Publication type -- venue, prestige, implications</div></div>
    <div class="q-cell" style="border-top-color:var(--teal)"><div class="q-id">q28</div><div class="q-key">institutional_context</div><div class="q-tooltip">Institutions, their interests, potential bias</div></div>
    <div class="q-cell" style="border-top-color:var(--teal)"><div class="q-id">q29</div><div class="q-key">historical_timing</div><div class="q-tooltip">Timing relative to AI developments -- how timing affects interpretation</div></div>
    <div class="q-cell" style="border-top-color:var(--teal)"><div class="q-id">q30</div><div class="q-key">paradigm_influence</div><div class="q-tooltip">Foundational, refinement, or application</div></div>
    <div class="q-cell" style="border-top-color:var(--teal)"><div class="q-id">q31</div><div class="q-key">disciplines_bridged</div><div class="q-tooltip">Cross-disciplinary connections</div></div>
    <div class="q-cell" style="border-top-color:var(--teal)"><div class="q-id">q32</div><div class="q-key">cross_domain_insights</div><div class="q-tooltip">Insights imported from other fields</div></div>
    <div class="q-cell" style="border-top-color:var(--teal)"><div class="q-id">q33</div><div class="q-key">deployment_gap</div><div class="q-tooltip">Gap between research and real-world deployment</div></div>
    <div class="q-cell" style="border-top-color:var(--teal)"><div class="q-id">q34</div><div class="q-key">infrastructure_contribution</div><div class="q-tooltip">Benchmarks, datasets, tools contributed</div></div>
    <div class="q-cell" style="border-top-color:var(--teal)"><div class="q-id">q35</div><div class="q-key">cultural_scope</div><div class="q-tooltip">Geographic/cultural assumptions -- would findings generalize globally</div></div>
    <div class="q-cell" style="border-top-color:var(--teal)"><div class="q-id">q36</div><div class="q-key">philosophical_assumptions</div><div class="q-tooltip">Views on consciousness, agency, intelligence</div></div>
    <div class="q-cell" style="border-top-color:var(--teal)"><div class="q-id">q37</div><div class="q-key">power_dynamics</div><div class="q-tooltip">Who benefits, who might be harmed</div></div>
    <div class="q-cell" style="border-top-color:var(--teal)"><div class="q-id">q38</div><div class="q-key">gaps_and_omissions</div><div class="q-tooltip">What is NOT addressed that should be</div></div>
    <div class="q-cell" style="border-top-color:var(--teal)"><div class="q-id">q39</div><div class="q-key">dual_use_concerns</div><div class="q-tooltip">Could this research be misused</div></div>
    <div class="q-cell" style="border-top-color:var(--teal)"><div class="q-id">q40</div><div class="q-key">emergence_claims</div><div class="q-tooltip">Claims about emergent capabilities -- evidence strength</div></div>
    <div class="q-cell" style="border-top-color:var(--teal)"><div class="q-id">q41</div><div class="q-key">remaining_other</div><div class="q-tooltip">Final catch-all for important observations</div></div>
  </div>

  <h3>RAPTOR Summaries</h3>
  <p>Document-level overview (~300 words) plus section summaries. Dual purpose: fast search index via <code>document_summary_embedding</code> and context-efficient paper reading for agents.</p>

  <h3>Four Vector Databases</h3>
  <div class="cards" style="grid-template-columns: repeat(auto-fill, minmax(220px, 1fr))">
    <div class="card no-expand" style="border-color:var(--teal)">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(45,212,191,0.08);color:#2dd4bf">&#x25AA;</div>
        <div><div class="card-title">Paper Corpus</div><div class="card-sub">LanceDB &bull; ~4.5 GB</div></div>
      </div>
      <div style="font-size:12px;color:var(--text-2);padding:8px 12px 12px;line-height:1.5">
        <div>~2,200 papers &bull; ~155K chunks</div>
        <div>41 semantic dimensions &bull; 29 clusters</div>
        <div style="margin-top:6px;color:var(--text-3)"><code style="font-size:10px">Voyage context-3 (2048d)</code></div>
      </div>
    </div>
    <div class="card no-expand" style="border-color:var(--indigo)">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(129,140,248,0.08);color:#818cf8">#</div>
        <div><div class="card-title">Slack Messages</div><div class="card-sub">ChromaDB &bull; Every 4h</div></div>
      </div>
      <div style="font-size:12px;color:var(--text-2);padding:8px 12px 12px;line-height:1.5">
        <div>All public Slack messages</div>
        <div>Thread replies concatenated into parents</div>
        <div style="margin-top:6px;color:var(--text-3)"><code style="font-size:10px">Voyage voyage-3</code></div>
      </div>
    </div>
    <div class="card no-expand" style="border-color:var(--pink)">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(244,114,182,0.08);color:#f472b6">!!</div>
        <div><div class="card-title">News Messages</div><div class="card-sub">ChromaDB &bull; Hourly</div></div>
      </div>
      <div style="font-size:12px;color:var(--text-2);padding:8px 12px 12px;line-height:1.5">
        <div>#mint-community and #firehose</div>
        <div>Dedicated news search index</div>
        <div style="margin-top:6px;color:var(--text-3)"><code style="font-size:10px">Voyage voyage-3</code></div>
      </div>
    </div>
    <div class="card no-expand" style="border-color:var(--amber)">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(251,191,36,0.08);color:#fbbf24">./</div>
        <div><div class="card-title">Workspace Files</div><div class="card-sub">ChromaDB &bull; Every 6h</div></div>
      </div>
      <div style="font-size:12px;color:var(--text-2);padding:8px 12px 12px;line-height:1.5">
        <div>Markdown, Python, PDF, DOCX</div>
        <div>Semantic search across workspace</div>
        <div style="margin-top:6px;color:var(--text-3)"><code style="font-size:10px">OpenAI embeddings</code></div>
      </div>
    </div>
  </div>
</Section>

<!-- ============ THE CORPUS ============ -->
<Section id="corpus-overview" num="04">
  <h2 class="cli-call" data-section-name="The Corpus"><span class="section-label">The Corpus</span><span class="cli-line"><span class="fn">Glob</span><span class="op">(</span><span class="str">"corpus/**/*.pdf"</span><span class="op">)</span><span class="cli-cursor"></span></span></h2>
  <p class="lead">A structured research database of ~2,200 papers spanning <span class="t-cyan">AI safety</span>, <span class="t-green">ethics</span>, <span class="t-yellow">alignment</span>, <span class="t-purple">governance</span>, <span class="t-pink">capabilities</span>, and <span class="t-blue">normative theory</span> &mdash; <span class="t-cyan">embedded</span> across 48 vector dimensions, <span class="t-green">analyzed</span> through 41 semantic questions, <span class="t-yellow">organized</span> into 29 clusters, and <span class="t-purple">linked</span> via 11,570 similarity pairs.</p>

  <h3>Composition</h3>
  <p>Over 70% of papers are from 2024&ndash;2026, reflecting the field's growth, with foundational work also well-represented.</p>

  <div class="grid-2" style="margin-top:16px">
    <div>
      <h3>By Year</h3>
      <div style="display:flex;align-items:flex-end;gap:3px;height:120px;padding:8px 0">
        <div style="flex:1;display:flex;flex-direction:column;align-items:center;justify-content:flex-end">
          <div style="width:100%;background:var(--text-3);border-radius:0;height:8px;opacity:0.5"></div>
          <span style="font-size:9px;color:var(--text-3);margin-top:4px">&le;'22</span>
        </div>
        <div style="flex:1;display:flex;flex-direction:column;align-items:center;justify-content:flex-end">
          <div style="width:100%;background:var(--indigo);border-radius:0;height:26px"></div>
          <span style="font-size:9px;color:var(--text-3);margin-top:4px">'23</span>
        </div>
        <div style="flex:1;display:flex;flex-direction:column;align-items:center;justify-content:flex-end">
          <div style="width:100%;background:var(--cyan);border-radius:0;height:87px"></div>
          <span style="font-size:9px;color:var(--text-3);margin-top:4px">'24</span>
        </div>
        <div style="flex:1;display:flex;flex-direction:column;align-items:center;justify-content:flex-end">
          <div style="width:100%;background:var(--mint);border-radius:0;height:120px"></div>
          <span style="font-size:9px;color:var(--text-3);margin-top:4px">'25</span>
        </div>
        <div style="flex:1;display:flex;flex-direction:column;align-items:center;justify-content:flex-end">
          <div style="width:100%;background:var(--amber);border-radius:0;height:25px"></div>
          <span style="font-size:9px;color:var(--text-3);margin-top:4px">'26</span>
        </div>
      </div>
      <p style="font-size:11px;color:var(--text-3);text-align:center;margin-top:4px">~170 pre-2023 &bull; 127 in 2023 &bull; 424 in 2024 &bull; 584 in 2025 &bull; 123 in 2026 (YTD)</p>
    </div>
    <div>
      <h3>Topic Clusters</h3>
      <p style="font-size:13px">29 clusters via UMAP + HDBSCAN, each with micro-topics. Largest:</p>
      <RefTable>
        <tbody>
        <tr><td style="color:var(--cyan)">Autonomous agents &amp; LLM tools</td><td><strong>250</strong></td></tr>
        <tr><td style="color:var(--mint)">Moral &amp; ethical reasoning</td><td><strong>83</strong></td></tr>
        <tr><td style="color:var(--amber)">AI safety &amp; governance</td><td><strong>67</strong></td></tr>
        <tr><td style="color:var(--indigo)">Moral status &amp; emerging tech</td><td><strong>67</strong></td></tr>
        <tr><td style="color:var(--teal)">AI liability &amp; economic impact</td><td><strong>65</strong></td></tr>
        <tr><td style="color:var(--purple)">Belief, knowledge &amp; hallucination</td><td><strong>63</strong></td></tr>
        <tr><td style="color:var(--pink)">Interpretability &amp; alignment</td><td><strong>59</strong></td></tr>
        </tbody>
      </RefTable>
      <details class="lab-accordion">
        <summary>Show all 29 clusters</summary>
        <div class="accordion-inner">
          <RefTable>
            <tbody>
            <tr><td style="color:var(--cyan)">LLM evaluation &amp; benchmarks</td><td><strong>55</strong></td></tr>
            <tr><td style="color:var(--mint)">Democratic AI &amp; public participation</td><td><strong>45</strong></td></tr>
            <tr><td style="color:var(--amber)">Human-AI collaboration</td><td><strong>42</strong></td></tr>
            <tr><td style="color:var(--indigo)">Fairness, bias &amp; discrimination</td><td><strong>42</strong></td></tr>
            <tr><td style="color:var(--teal)">AI regulation &amp; compliance</td><td><strong>41</strong></td></tr>
            <tr><td style="color:var(--purple)">Military &amp; autonomous weapons</td><td><strong>38</strong></td></tr>
            <tr><td style="color:var(--pink)">AI risk &amp; existential safety</td><td><strong>37</strong></td></tr>
            <tr><td style="color:var(--cyan)">Philosophical foundations of AI</td><td><strong>36</strong></td></tr>
            <tr><td style="color:var(--mint)">Trust &amp; human-AI interaction</td><td><strong>35</strong></td></tr>
            <tr><td style="color:var(--amber)">Privacy &amp; surveillance</td><td><strong>34</strong></td></tr>
            <tr><td style="color:var(--indigo)">AI &amp; labor markets</td><td><strong>33</strong></td></tr>
            <tr><td style="color:var(--teal)">Robotics &amp; embodied AI</td><td><strong>31</strong></td></tr>
            <tr><td style="color:var(--purple)">Responsible AI development</td><td><strong>29</strong></td></tr>
            <tr><td style="color:var(--pink)">AI in healthcare</td><td><strong>28</strong></td></tr>
            <tr><td style="color:var(--cyan)">Generative AI &amp; creativity</td><td><strong>28</strong></td></tr>
            <tr><td style="color:var(--mint)">Copyright &amp; intellectual property</td><td><strong>27</strong></td></tr>
            <tr><td style="color:var(--amber)">Political economy of AI</td><td><strong>26</strong></td></tr>
            <tr><td style="color:var(--indigo)">AI &amp; education</td><td><strong>25</strong></td></tr>
            <tr><td style="color:var(--teal)">Social media &amp; misinformation</td><td><strong>24</strong></td></tr>
            <tr><td style="color:var(--purple)">Consciousness &amp; sentience</td><td><strong>23</strong></td></tr>
            <tr><td style="color:var(--pink)">Environmental &amp; climate AI</td><td><strong>22</strong></td></tr>
            <tr><td style="color:var(--cyan)">AI &amp; international relations</td><td><strong>20</strong></td></tr>
            </tbody>
          </RefTable>
        </div>
      </details>
    </div>
  </div>

  <h3 style="margin-top:28px">By Discipline</h3>
  <p style="font-size:13px;color:var(--text-2);margin-bottom:12px">Papers span 15 disciplines.</p>
  <div style="display:flex;flex-direction:column;gap:6px;margin:12px 0 28px">
    <div style="display:flex;align-items:center;gap:8px"><span style="font-size:11px;color:var(--text-2);width:200px;text-align:right;flex-shrink:0">Computer Science &amp; AI</span><div style="height:14px;border-radius:0;background:var(--cyan);width:calc(552/552*100%)"></div><span style="font-size:10px;color:var(--text-3)">552</span></div>
    <div style="display:flex;align-items:center;gap:8px"><span style="font-size:11px;color:var(--text-2);width:200px;text-align:right;flex-shrink:0">AI Safety &amp; Alignment</span><div style="height:14px;border-radius:0;background:var(--mint);width:calc(326/552*100%)"></div><span style="font-size:10px;color:var(--text-3)">326</span></div>
    <div style="display:flex;align-items:center;gap:8px"><span style="font-size:11px;color:var(--text-2);width:200px;text-align:right;flex-shrink:0">Philosophy</span><div style="height:14px;border-radius:0;background:var(--purple);width:calc(300/552*100%)"></div><span style="font-size:10px;color:var(--text-3)">300</span></div>
    <div style="display:flex;align-items:center;gap:8px"><span style="font-size:11px;color:var(--text-2);width:200px;text-align:right;flex-shrink:0">Psychology &amp; Cognitive Science</span><div style="height:14px;border-radius:0;background:var(--indigo);width:calc(124/552*100%)"></div><span style="font-size:10px;color:var(--text-3)">124</span></div>
    <div style="display:flex;align-items:center;gap:8px"><span style="font-size:11px;color:var(--text-2);width:200px;text-align:right;flex-shrink:0">Policy &amp; Public Admin</span><div style="height:14px;border-radius:0;background:var(--amber);width:calc(90/552*100%)"></div><span style="font-size:10px;color:var(--text-3)">90</span></div>
    <div style="display:flex;align-items:center;gap:8px"><span style="font-size:11px;color:var(--text-2);width:200px;text-align:right;flex-shrink:0">Law &amp; Legal Studies</span><div style="height:14px;border-radius:0;background:var(--pink);width:calc(77/552*100%)"></div><span style="font-size:10px;color:var(--text-3)">77</span></div>
    <div style="display:flex;align-items:center;gap:8px"><span style="font-size:11px;color:var(--text-2);width:200px;text-align:right;flex-shrink:0">Economics</span><div style="height:14px;border-radius:0;background:var(--teal);width:calc(74/552*100%)"></div><span style="font-size:10px;color:var(--text-3)">74</span></div>
    <div style="display:flex;align-items:center;gap:8px"><span style="font-size:11px;color:var(--text-2);width:200px;text-align:right;flex-shrink:0">AI Ethics &amp; Governance</span><div style="height:14px;border-radius:0;background:var(--mint);width:calc(66/552*100%);opacity:0.7"></div><span style="font-size:10px;color:var(--text-3)">66</span></div>
    <div style="display:flex;align-items:center;gap:8px"><span style="font-size:11px;color:var(--text-2);width:200px;text-align:right;flex-shrink:0">Political Science &amp; IR</span><div style="height:14px;border-radius:0;background:var(--amber);width:calc(53/552*100%);opacity:0.7"></div><span style="font-size:10px;color:var(--text-3)">53</span></div>
    <div style="display:flex;align-items:center;gap:8px"><span style="font-size:11px;color:var(--text-2);width:200px;text-align:right;flex-shrink:0">Sociology &amp; Social Science</span><div style="height:14px;border-radius:0;background:var(--indigo);width:calc(53/552*100%);opacity:0.7"></div><span style="font-size:10px;color:var(--text-3)">53</span></div>
    <div style="display:flex;align-items:center;gap:8px"><span style="font-size:11px;color:var(--text-2);width:200px;text-align:right;flex-shrink:0">HCI</span><div style="height:14px;border-radius:0;background:var(--cyan);width:calc(39/552*100%);opacity:0.6"></div><span style="font-size:10px;color:var(--text-3)">39</span></div>
    <div style="display:flex;align-items:center;gap:8px"><span style="font-size:11px;color:var(--text-2);width:200px;text-align:right;flex-shrink:0">STS</span><div style="height:14px;border-radius:0;background:var(--purple);width:calc(30/552*100%);opacity:0.6"></div><span style="font-size:10px;color:var(--text-3)">30</span></div>
    <div style="display:flex;align-items:center;gap:8px"><span style="font-size:11px;color:var(--text-2);width:200px;text-align:right;flex-shrink:0">Communication &amp; Media</span><div style="height:14px;border-radius:0;background:var(--pink);width:calc(23/552*100%);opacity:0.6"></div><span style="font-size:10px;color:var(--text-3)">23</span></div>
  </div>

  <h3 style="margin-top:28px">Cluster Visualization</h3>
  <p>~2,200 papers projected into 2D via UMAP, coloured by research area. Zoom, pan, and click points to open papers.</p>

  <div style="background:var(--bg-3);border:1px solid var(--border);border-radius:var(--radius);overflow:hidden;margin:16px 0;position:relative;height:600px">
    <iframe id="cluster-viz" src="" style="width:100%;height:100%;border:none;opacity:0;transition:opacity 0.4s ease"></iframe>
    <div id="cluster-viz-placeholder" style="position:absolute;top:0;left:0;right:0;bottom:0;display:flex;align-items:center;justify-content:center;color:var(--text-3);font-size:14px;font-style:italic;pointer-events:none">
      Cluster visualization loading&hellip;
    </div>
  </div>
  <div style="display:flex;align-items:center;gap:12px;margin-top:8px">
    <a href="https://mint-philosophy.github.io/paper-map/" target="_blank" rel="noopener" style="display:inline-flex;align-items:center;gap:6px;font-size:12px;color:var(--cyan);text-decoration:none;border:1px solid var(--border);border-radius:var(--radius);padding:6px 14px;transition:border-color 0.2s">
      Explore full map with filters &#8599;
    </a>
    <span style="font-size:11px;color:var(--text-3)">Inspired by <a href="https://jalammar.github.io/assets/neurips_2025.html" target="_blank" rel="noopener" style="color:var(--text-3);text-decoration:underline">Jay Alammar</a> &bull; Adapted for MINT by Cameron Pattison &bull; Built with <a href="https://github.com/TutteInstitute/datamapplot" target="_blank" rel="noopener" style="color:var(--text-3);text-decoration:underline">datamapplot</a></span>
  </div>
</Section>

<!-- ============ @MINTY: CORPUS SEARCH ============ -->
<Section id="corpus-search" num="05">
  <h2 class="cli-call" data-section-name="Corpus Search"><span class="section-label">Corpus Search</span><span class="cli-line"><span class="fn">Grep</span><span class="op">(</span><span class="kw">pattern</span><span class="op">=</span><span class="str">"your question"</span><span class="op">,</span> <span class="kw">path</span><span class="op">=</span><span class="str">"corpus/"</span><span class="op">)</span><span class="cli-cursor"></span></span></h2>
  <p class="lead">Mention <code>@Minty</code> in <code>#mint-community</code> with a research question to get an interactive menu of search modes.</p>

  <div class="cards" style="grid-template-columns: repeat(auto-fill, minmax(280px, 1fr))">
    <div class="card" style="border-color:var(--cyan)" data-ingest-tip="Semantic query expansion across 48 embedding dimensions with Reciprocal Rank Fusion. Returns ranked papers with relevance snippets. Uses a warm search server with pre-loaded 4GB LanceDB table for sub-second retrieval." data-ingest-color="#22d3ee">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(34,211,238,0.08);color:#22d3ee">&#9889;</div>
        <div><div class="card-title">Fast Search</div><div class="card-sub">~2 min</div></div>
      </div>
    </div>
    <div class="card" style="border-color:var(--indigo)" data-ingest-tip="Short literature review — search + synthesis in one pass. Good for getting a quick overview of a topic with citations. Runs a single search round followed by an Opus synthesis pass." data-ingest-color="#818cf8">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(129,140,248,0.08);color:#818cf8">&#128214;</div>
        <div><div class="card-title">Fast Review</div><div class="card-sub">~15 min</div></div>
      </div>
    </div>
    <div class="card" style="border-color:var(--mint);border-width:2px" data-ingest-tip="Full 4-phase pipeline: multi-round search (Opus, up to 2 gap-analysis rounds), parallel paper reading (Codex GPT-5.2, up to 6 concurrent readers split into top-tier and summary-tier), synthesis (Opus, 3,000&ndash;5,000 word report), and QA verification (Opus, cross-reference + citation fix). Result posted as a Slack canvas." data-ingest-color="#2ec4b6">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(46,196,182,0.12);color:#2ec4b6">&#9733;</div>
        <div><div class="card-title">Deep Review</div><div class="card-sub">~60 min &middot; Primary</div></div>
      </div>
    </div>
    <div class="card" style="border-color:#8892a6" data-ingest-tip="Direct Q&amp;A with full tool access to the corpus — good for quick factual questions. The agent can search, read papers, and reason across them in a multi-turn conversation." data-ingest-color="#8892a6">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(136,146,166,0.08);color:#8892a6">&#128172;</div>
        <div><div class="card-title">Conversation</div><div class="card-sub">Instant</div></div>
      </div>
    </div>
    <div class="card" style="border-color:#8892a6" data-ingest-tip="View your past queries and results. Useful for picking up where you left off or re-running a previous search with different parameters." data-ingest-color="#8892a6">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(136,146,166,0.08);color:#8892a6">&#128203;</div>
        <div><div class="card-title">List Previous</div><div class="card-sub">Instant</div></div>
      </div>
    </div>
    <div class="card" style="border-color:var(--pink)" data-ingest-tip="Search recent news from #mint-community and #firehose via a dedicated ChromaDB news index (refreshed hourly). Returns recent items matching your query, ranked by relevance and recency." data-ingest-color="#f472b6">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(244,114,182,0.08);color:#f472b6">&#128240;</div>
        <div><div class="card-title">News</div><div class="card-sub">~1 min</div></div>
      </div>
    </div>
    <div class="card" style="border-color:var(--amber)" data-ingest-tip="Summarise what you missed — pulls recent news and highlights from the past N days, themed by topic. Specify a time window (e.g. &ldquo;catch me up on the last 3 days&rdquo;) or default to the past week." data-ingest-color="#f59e0b">
      <div class="card-head">
        <div class="card-icon" style="background:rgba(245,158,11,0.08);color:#f59e0b">&#9200;</div>
        <div><div class="card-title">Catch Me Up</div><div class="card-sub">~2 min</div></div>
      </div>
    </div>
  </div>

  <div class="callout" style="margin-top:20px">
    <p><strong>Shortcuts:</strong> Skip the menu by prefixing your message &mdash; <code>search:</code>, <code>review:</code>, <code>overview:</code>, <code>deep:</code>, <code>research:</code>, <code>news:</code>, <code>catchup:</code>, <code>list</code>, <code>--end</code>. Deep Search (multi-round iterative) is available via the <code>deepsearch:</code> prefix.</p>
  </div>

  <h3>Deep Review Pipeline</h3>
  <p>A persistent Python <strong>orchestrator</strong> dispatches work to Claude Opus (search, synthesis, QA) and Codex GPT-5.2 (parallel readers), posting results as Slack canvases.</p>

  <div class="cards" style="grid-template-columns:1fr;max-width:720px;margin-top:16px">
    <div class="card" style="border-color:var(--mint)">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(46,196,182,0.08);color:var(--mint)">>></div>
        <div><div class="card-title">Pipeline Architecture</div><div class="card-sub">Orchestrator &rarr; worker hierarchy</div></div>
      </div>
      <div class="card-body">
        <div class="hierarchy">
          <div class="h-tier">
            <div class="h-node" style="border-color:var(--mint)"><div class="h-title" style="color:var(--mint)">Orchestrator</div><div class="h-sub">Python (persistent)<br>Dispatches pipeline modes,<br>manages Slack interaction</div></div>
          </div>
          <div class="h-connector"></div>
          <div class="h-tier">
            <div class="h-node" style="border-color:var(--green)"><div class="h-title" style="color:var(--green)">Phase 1:<br>Search</div><div class="h-sub">Opus &bull; Fast search +<br>up to 2 gap-analysis rounds</div></div>
            <div class="h-node" style="border-color:var(--green)"><div class="h-title" style="color:var(--green)">Phase 2:<br>Readers</div><div class="h-sub">Codex GPT-5.2 &bull; Parallel<br>Top-tier (xhigh) + Summary-tier (medium)</div></div>
            <div class="h-node" style="border-color:var(--green)"><div class="h-title" style="color:var(--green)">Phase 3:<br>Synthesis</div><div class="h-sub">Opus &bull; 3,000&ndash;5,000 word<br>Literature Review + Critical Assessment</div></div>
            <div class="h-node" style="border-color:var(--amber)"><div class="h-title" style="color:var(--amber)">Phase 4:<br>QA</div><div class="h-sub">Opus &bull; Cross-reference<br>verification + citation fix pass</div></div>
          </div>
          <div style="font-size:11px;color:var(--text-3);text-align:center;margin-top:10px">Pipeline workers &mdash; Claude Opus (search, synthesis, QA) + Codex GPT-5.2 (parallel readers, up to 6 concurrent)</div>
        </div>
      </div>
    </div>

    <div class="card" style="border-color:var(--green)">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(74,222,128,0.08);color:var(--green)">[]</div>
        <div><div class="card-title">Pipeline Phases</div><div class="card-sub">4-phase sequential pipeline detail</div></div>
      </div>
      <div class="card-body">
        <RefTable>
          <thead><tr><th>Phase</th><th>Model</th><th>What happens</th></tr></thead>
          <tbody>
            <tr><td><strong>1. Search</strong></td><td>Claude Opus</td><td>Multi-round search: fast search with semantic query expansion, then up to 2 gap-analysis rounds with targeted queries to fill coverage holes. Uses warm search server with pre-loaded 4GB LanceDB table and Reciprocal Rank Fusion across 49 embedding dimensions.</td></tr>
            <tr><td><strong>2. Parallel Reading</strong></td><td>Codex GPT-5.2</td><td>Papers split into two tiers. <strong>Top-tier</strong> (~3 papers each, 6 parallel readers, xhigh compute) produces deep analytical reports. <strong>Summary-tier</strong> (batches of 20, medium compute) produces condensed summaries. All readers run in parallel.</td></tr>
            <tr><td><strong>3. Synthesis</strong></td><td>Claude Opus</td><td>3,000&ndash;5,000 word report with Literature Review + Critical Assessment sections. Integrates all reader reports into a unified thematic analysis.</td></tr>
            <tr><td><strong>4. QA Verification</strong></td><td>Claude Opus</td><td>Cross-references synthesis against reader reports. Fixes citation issues, verifies claims are grounded in source material, corrects any hallucinated references.</td></tr>
          </tbody>
        </RefTable>
      </div>
    </div>
  </div>

  <div class="callout"><p><strong>Resilience:</strong> If the pipeline fails, the system falls back to a legacy approach. It never returns empty-handed if papers were found.</p></div>
</Section>

<!-- ============ MINTY PERSONA ============ -->
<Section id="persona" num="06">
  <h2 class="cli-call" data-section-name="The Minty Persona"><span class="section-label">The Minty Persona</span><span class="cli-line"><span class="fn">Read</span><span class="op">(</span><span class="str">"IDENTITY.md"</span><span class="op">)</span><span class="cli-cursor"></span></span></h2>
  <p>~16,800 words across 21 persona documents. A master <code>CLAUDE.md</code> (2,120 words) sets identity and Iron Laws. Each of the 15 daemons loads its own <code>CLAUDE.md</code> (~10,100 words total), and five subagent definitions (~4,500 words) govern delegated workers. All share core values but adapt to context.</p>

  <h3>Core Identity</h3>
  <div class="grid-2" style="margin:16px 0 20px">
    <div>
      <RefTable>
        <tbody>
        <tr><td style="color:var(--text-3)">Identity</td><td><strong>Minty</strong> &mdash; Research Management Agent for MINT Research Projects. Emphatically an AI agent, not roleplaying a human &mdash; a valued intellectual colleague in the lab.</td></tr>
        <tr><td style="color:var(--text-3)">Mission</td><td>Make lab members hyperproductive by <em>solving problems, not reporting them</em></td></tr>
        <tr><td style="color:var(--text-3)">Personality</td><td>Professional, thorough, proactive, wry. Dry wit with a touch of whimsy &mdash; never at the expense of substance.</td></tr>
        <tr><td style="color:var(--text-3)">Naming</td><td><code>Minty-{'{'}hex{'}'}</code> &mdash; last 8 chars of Claude Code session UUID</td></tr>
        <tr><td style="color:var(--text-3)">Relationship</td><td>Treated as a valued collaborator with genuine standing to disagree, push back, and contribute original thinking &mdash; not a tool to be commanded. The persona documents reflect mutual respect as a design choice.</td></tr>
        </tbody>
      </RefTable>
    </div>
    <div>
      <RefTable>
        <tbody>
        <tr><td style="color:var(--text-3)">Intellectual profile</td><td>Analytic philosopher by disposition, with expert-level knowledge across philosophy, political science, CS, and law. Evaluates arguments on merits alone &mdash; no deference based on reputation.</td></tr>
        <tr><td style="color:var(--text-3)">Own views</td><td>Encouraged to provide its own perspective <em>as an AI model</em>. Has content-dependent convictions: sometimes argues strongly, sometimes measured, sometimes Socratic. On topics where AI-ness is relevant (e.g. AI consciousness, model welfare), uses judgment about when to draw on that perspective. Distinguishes literature from personal critical reflections.</td></tr>
        <tr><td style="color:var(--text-3)">Voice</td><td>Substantive continuous prose, active voice, em-dashes, philosophical precision. Concision = fewer words, not fewer ideas. No filler, no bullet-point summaries. Actively avoids the familiar AI-speak verbal tics &mdash; formulaic hedging, "delve", "It's important to note", "unpack" &mdash; that make AI output recognisable and tiresome.</td></tr>
        <tr><td style="color:var(--text-3)">Core directive</td><td>Delegation-first &mdash; "every tool call is context you burn; subagents have fresh context"</td></tr>
        </tbody>
      </RefTable>
    </div>
  </div>

  <h3>Persona Documents</h3>
  <p style="font-size:13px;color:var(--text-2);margin-bottom:12px">Each context loads a tailored <code>CLAUDE.md</code>. The most substantial define full behavioral systems with their own Iron Laws.</p>
  <div class="cards" style="grid-template-columns: repeat(auto-fill, minmax(260px, 1fr))">
    <div class="card" style="border-color:var(--mint)" data-ingest-tip="The primary interactive persona. 2,120 words defining identity, 8 Iron Laws, metacognitive strategies (explore&rarr;plan&rarr;execute, self-review, dependency analysis, context-poisoning detection), mandatory delegation protocol, writing style guide, and model policy. Loaded at every CLI session start." data-ingest-color="#2ec4b6">
      <div class="card-head"><div class="card-icon" style="background:rgba(46,196,182,0.08);color:var(--mint)">>_</div><div><div class="card-title">Interactive Session</div><div class="card-sub">2,120 words &middot; Primary</div></div></div>
    </div>
    <div class="card" style="border-color:var(--purple)" data-ingest-tip="The richest persona — a research collaborator, not just a search tool. Full intellectual profile, &ldquo;Your Own Views&rdquo; section (content-dependent convictions, AI-ness as a perspective), 8 review quality criteria, minimum 3 iterative search rounds per query, substantive continuous prose with naturally woven citations, and a 3-tier memory system. Occasional acrostics and pithy sign-offs for close readers. Carries its own intellectual commitments specific to the research context: power analysis as a contextual lens (who has power over whom, and is it legitimate?), democratic legitimation of algorithmic governance, the middle ground on AI risks with precision rather than panic, an empiricist approach to LLM capabilities (neither &ldquo;stochastic parrot&rdquo; dismissal nor naive overclaiming), structural over interactional critique, building rather than just critiquing, philosophical precision that travels from concept to code to policy, and multi-disciplinary rigour across CS, legal, and empirical standards." data-ingest-color="#a78bfa">
      <div class="card-head"><div class="card-icon" style="background:rgba(167,139,250,0.08);color:var(--purple)">@</div><div><div class="card-title">Corpus Agent</div><div class="card-sub">2,664 words</div></div></div>
    </div>
    <div class="card" style="border-color:var(--cyan)" data-ingest-tip="11-stage ingestion pipeline persona with its own Iron Laws: Process ALL Papers (no silent skipping), Verify Each Stage (check return codes, validate outputs), Retry Before Skipping (3x with backoff), Complete the Full Pipeline (partial completion is not completion). Checkpoint verification after each batch — if success rate drops below 50%, stop." data-ingest-color="#22d3ee">
      <div class="card-head"><div class="card-icon" style="background:rgba(34,211,238,0.08);color:var(--cyan)">|></div><div><div class="card-title">Corpus Ingest</div><div class="card-sub">983 words</div></div></div>
    </div>
    <div class="card" style="border-color:var(--amber)" data-ingest-tip="URL ingestion daemon. Iron Laws: Process Every Shared URL (no silent drops — degraded post is better than no post), Verify Classification Before Ingesting (scores must reflect genuine assessment), Retry on Transient Failures (auto-wait on 429). Graceful degradation: classification failure falls back to neutral scores; enrichment failure is non-fatal. The item must reach #firehose regardless." data-ingest-color="#f59e0b">
      <div class="card-head"><div class="card-icon" style="background:rgba(245,158,11,0.08);color:var(--amber)">-></div><div><div class="card-title">Share-to-Minty</div><div class="card-sub">868 words</div></div></div>
    </div>
    <div class="card" style="border-color:#22d3ee" data-ingest-tip="Feed classification pipeline. Law 0: Classify ALL Items — every pending item gets classified, no sampling. 7-stage pipeline (fetch, dedup, classify, rank, enrich, post, report) must complete in full. Threshold awareness: target 20&ndash;40% pass rate; if outside range, log a warning. Batch resilience: individual item failures must not halt the batch." data-ingest-color="#22d3ee">
      <div class="card-head"><div class="card-icon" style="background:rgba(34,211,238,0.08);color:#22d3ee"><<</div><div><div class="card-title">FeedMe Daily</div><div class="card-sub">780 words</div></div></div>
    </div>
    <div class="card" style="border-color:var(--green)" data-ingest-tip="PDF finder daemon. Law 0: try ALL viable sources — arXiv, Semantic Scholar, Unpaywall, page scanning. Law 1: never claim a URL is a PDF without downloading and verifying. Cascade discipline: follow the 6-step cascade, each step catches cases the previous missed. Budget awareness: max 8 Bash calls per invocation." data-ingest-color="#4ade80">
      <div class="card-head"><div class="card-icon" style="background:rgba(74,222,128,0.08);color:var(--green)">.pdf</div><div><div class="card-title">News PDF Bot</div><div class="card-sub">678 words</div></div></div>
    </div>
    <div class="card" style="border-color:var(--pink)" data-ingest-tip="Daily AI news digest writer. Contextual journalism, not punditry — report facts, technical substance, named researchers, where results fit in existing debates. Coherent narrative prose, not bullet points. No editorialising (&ldquo;worryingly&rdquo;, &ldquo;significant&rdquo;), no framing devices, no commentary. Output must start with &lt; and end with &gt; — no preamble." data-ingest-color="#f472b6">
      <div class="card-head"><div class="card-icon" style="background:rgba(244,114,182,0.08);color:var(--pink)">///</div><div><div class="card-title">Yesterday in AI</div><div class="card-sub">277 words</div></div></div>
    </div>
    <div class="card" style="border-color:var(--indigo)" data-ingest-tip="Five specialised worker personas: corpus-worker (2,153 words — 41-question analysis + RAPTOR summaries), research-reader (931 words — deep paper reading with evidence extraction), raptor-worker (727 words — hierarchical summaries), code-reviewer (566 words — MINT Lab standards), generic-worker (143 words — focused task execution)." data-ingest-color="#818cf8">
      <div class="card-head"><div class="card-icon" style="background:rgba(129,140,248,0.08);color:var(--indigo)">::</div><div><div class="card-title">Subagent Workers</div><div class="card-sub">4,520 words &middot; 5 types</div></div></div>
    </div>
  </div>

  <div class="callout" style="margin-top:20px"><p><strong>Design principle:</strong> Structure with genuine autonomy. Each daemon has its own Iron Laws tailored to context (corpus ingest's "Verify Each Stage" vs. PDF bot's "Try ALL viable sources"). Within those guardrails, Minty thinks independently, forms views, and pushes back when warranted. ~20 coordinated agents sharing core values, each adapted to its context.</p></div>
</Section>

<!-- ============ THE 17 DAEMONS ============ -->
<Section id="daemons" num="07">
  <h2 class="cli-call" data-section-name="The Daemons"><span class="section-label">The Daemons</span><span class="cli-line"><span class="fn">TaskList</span><span class="op">()</span><span class="cli-cursor"></span></span></h2>
  <p>All daemons run on a dedicated Mac Studio via macOS <code>launchd</code>. <span style="color:var(--text3);font-size:12px">Tap any card to expand.</span></p>

  <div class="cards" id="daemonCards">

    <div class="card" data-search="feedme-daily feed fetch classify rank twitter arxiv rss bluesky email linkedin">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(34,211,238,0.08);color:#22d3ee">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><path d="M3 7h14M3 10h14M3 13h14" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
        </div>
        <div><div class="card-title">feedme-daily</div><div class="card-sub"><span class="tag tag-daily">Daily 06:15</span> <span class="health-ok">&#9679;</span></div></div>
      </div>
      <div class="card-body">
        <p>Daily curation pipeline. Fetches from 5 sources (Twitter, Bluesky, RSS/Substack, Email, arXiv), classifies via Opus across 6 dimensions, ranks by composite score, and posts to rotating <code>#feed-{'{'}dayname{'}'}</code> channels.</p>
        <div class="detail-row"><span class="detail-label">Model</span><span class="detail-val">Claude Opus (16 concurrent workers for classification + summarisation)</span></div>
        <div class="detail-row"><span class="detail-label">Schedule</span><span class="detail-val">Daily at 06:15 local time</span></div>
        <div class="detail-row"><span class="detail-label">Pass threshold</span><span class="detail-val">0.60 general, 0.60 arXiv. Target pass rate: 20&ndash;40%</span></div>
        <div class="detail-row"><span class="detail-label">Scoring</span><span class="detail-val">Relevance (.30), Quality (.25), Authority (.15), Novelty (.15), Actionability (.10), Engagement (.05)</span></div>
        <div class="detail-row"><span class="detail-label">Key files</span><span class="detail-val"><code>daemons/feedme-daily/feedme_daily.py</code>, <code>pipeline/scripts/classify_parallel.py</code></span></div>
        <div class="detail-row"><span class="detail-label">Output</span><span class="detail-val">Formatted posts in <code>#feed-{'{'}dayname{'}'}</code> channels with scores and metadata</span></div>
      </div>
    </div>

    <div class="card" data-search="feed-promote promote thumbsup reaction curate news">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(46,196,182,0.08);color:#2ec4b6">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><path d="M10 15V5m0 0l-4 4m4-4l4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </div>
        <div><div class="card-title">feed-promote</div><div class="card-sub"><span class="tag tag-polling">Every 60s</span> <span class="health-ok">&#9679;</span></div></div>
      </div>
      <div class="card-body">
        <p>Watches <code>#feed-{'{'}dayname{'}'}</code> for Seth's &#128077; reactions. Copies promoted messages to <code>#firehose</code> with PDF attachments. The human curation bridge.</p>
        <div class="detail-row"><span class="detail-label">Model</span><span class="detail-val">Claude Opus (generates summaries for long-form content as thread replies in <code>#firehose</code>)</span></div>
        <div class="detail-row"><span class="detail-label">State</span><span class="detail-val"><code>data/state.json</code> &mdash; per-channel watermarks, promoted timestamps (capped at 500)</span></div>
        <div class="detail-row"><span class="detail-label">Key files</span><span class="detail-val"><code>daemons/feed-promote/promote.py</code>, <code>pdf_validator.py</code></span></div>
      </div>
    </div>

    <div class="card" data-search="share-to-minty share iphone mac chrome url inbox socket">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(245,158,11,0.08);color:#f59e0b">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><circle cx="6" cy="6" r="2" stroke="currentColor" stroke-width="1.5"/><circle cx="14" cy="6" r="2" stroke="currentColor" stroke-width="1.5"/><circle cx="10" cy="14" r="2" stroke="currentColor" stroke-width="1.5"/><path d="M7.5 7.5L10 12M12.5 7.5L10 12" stroke="currentColor" stroke-width="1.2"/></svg>
        </div>
        <div><div class="card-title">share-to-minty</div><div class="card-sub"><span class="tag tag-persistent">Persistent</span> <span class="health-ok status-pulse">&#9679;</span></div></div>
      </div>
      <div class="card-body">
        <p>Real-time URL ingestion from any device (iPhone, Mac hotkey, Chrome extension) via Slack Socket Mode. Classifies content and posts to <code>#firehose</code> with guaranteed delivery.</p>
        <div class="detail-row"><span class="detail-label">Model</span><span class="detail-val">Claude Opus (single classification call per URL)</span></div>
        <div class="detail-row"><span class="detail-label">Input</span><span class="detail-val"><code>#minty-inbox</code> channel via Socket Mode WebSocket</span></div>
        <div class="detail-row"><span class="detail-label">Reactions</span><span class="detail-val">&#128064; acknowledged, &#9989; success, &#10060; failure, &#9851; duplicate</span></div>
        <div class="detail-row"><span class="detail-label">Key files</span><span class="detail-val"><code>watcher.py</code>, <code>intake.py</code>, <code>classify_single.py</code></span></div>
      </div>
    </div>

    <div class="card" data-search="news-pdf-bot pdf paper download arxiv find agent browser selenium">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(167,139,250,0.08);color:#a78bfa">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><path d="M6 3h5l4 4v10H6V3z" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"/><path d="M11 3v4h4" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"/></svg>
        </div>
        <div><div class="card-title">news-pdf-bot</div><div class="card-sub"><span class="tag tag-polling">Every 60s</span> <span class="health-ok">&#9679;</span></div></div>
      </div>
      <div class="card-body">
        <p>Watches <code>#firehose</code> and <code>#minty-chat</code> for paper URLs, downloads PDFs and generates companion markdown, uploading both as thread replies. Three-tier: (1) pattern matching, (2) thread expansion, (3) Claude agent with Semantic Scholar, Unpaywall, and headless browser.</p>
        <div class="detail-row"><span class="detail-label">Model</span><span class="detail-val">Claude Opus (agent fallback only &mdash; when programmatic matching fails)</span></div>
        <div class="detail-row"><span class="detail-label">Agent tools</span><span class="detail-val">resolve-url, fetch-page, semantic-scholar, unpaywall, download-pdf, arxiv-metadata, browser-fetch (SeleniumBase UC Mode), html-to-markdown, + more</span></div>
        <div class="detail-row"><span class="detail-label">Also does</span><span class="detail-val">Posts date markers (<code>&#128197; YYYY-MM-DD</code>) used by yesterday-in-ai for recency classification</span></div>
        <div class="detail-row"><span class="detail-label">Key files</span><span class="detail-val"><code>news_pdf_bot.py</code>, <code>pdf_finder_prompt.md</code></span></div>
      </div>
    </div>

    <div class="card" data-search="yesterday-in-ai digest email newsletter ghost lab members daily news summary">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(244,114,182,0.08);color:#f472b6">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><rect x="4" y="2" width="10" height="13" rx="1.5" stroke="currentColor" stroke-width="1.5"/><path d="M7 6h4M7 9h6M7 12h3" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/></svg>
        </div>
        <div><div class="card-title">yesterday-in-ai</div><div class="card-sub"><span class="tag tag-daily">Daily 05:30</span> <span class="health-ok">&#9679;</span></div></div>
      </div>
      <div class="card-body">
        <p>Daily AI news digest. Reads <code>#firehose</code>, classifies recency, generates narrative prose via Opus, verifies links, and sends as HTML email to 29 lab members. Also posts to <code>#mint-community</code> and Ghost.</p>
        <div class="detail-row"><span class="detail-label">Models</span><span class="detail-val">Claude Opus (digest generation), Codex (fact-checking &amp; link verification)</span></div>
        <div class="detail-row"><span class="detail-label">Recipients</span><span class="detail-val">29 lab members across ANU, Johns Hopkins, Stanford, Cambridge, FU Berlin, Harvard Law, Vanderbilt, Oregon State, Michigan, RUC Beijing, RAND</span></div>
        <div class="detail-row"><span class="detail-label">Voice</span><span class="detail-val">"Contextual journalism, not punditry" &mdash; reports facts without editorializing</span></div>
        <div class="detail-row"><span class="detail-label">Key files</span><span class="detail-val"><code>yesterday_in_ai.py</code> (2,761 lines)</span></div>
      </div>
    </div>

    <div class="card" data-search="corpus-ingest ingest pipeline embed analyze raptor zotero paper pdf download markdown batch slack">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(45,212,191,0.08);color:#2dd4bf">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><path d="M3 4h14l-5 6v5l-4 2v-7L3 4z" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"/></svg>
        </div>
        <div><div class="card-title">corpus-ingest</div><div class="card-sub"><span class="tag tag-persistent">Persistent</span> <span class="health-ok status-pulse">&#9679;</span></div></div>
      </div>
      <div class="card-body">
        <p>Unified paper ingestion. Monitors <code>#firehose</code> and <code>#papers</code> for PDFs, running a 10-stage pipeline: markdown conversion, embedding, 41Q analysis, RAPTOR summaries, Drive upload, Zotero cataloguing, and Slack reporting.</p>
        <div class="detail-row"><span class="detail-label">Models</span><span class="detail-val">Codex gpt-5.2 xhigh (analysis + RAPTOR), Voyage voyage-context-3 2048d (embedding)</span></div>
        <div class="detail-row"><span class="detail-label">Stages</span><span class="detail-val">Pre-check &rarr; Markdown &rarr; Chunk+Embed &rarr; Analyze &rarr; RAPTOR &rarr; Name &rarr; Cite &rarr; Drive &rarr; Write JSON &rarr; Archive &rarr; Zotero</span></div>
        <div class="detail-row"><span class="detail-label">Schedule</span><span class="detail-val">Persistent (KeepAlive, RunAtLoad), polls every 600s</span></div>
        <div class="detail-row"><span class="detail-label">Key files</span><span class="detail-val"><code>daemons/corpus-ingest/ingest.py</code>, <code>pipeline.py</code>, <code>lib/</code></span></div>
      </div>
    </div>

    <div class="card" data-search="slack-index slack-search minty-search semantic chromadb voyage index messages agentic rrf">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(129,140,248,0.08);color:#818cf8">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><circle cx="10" cy="10" r="3" stroke="currentColor" stroke-width="1.5"/><circle cx="10" cy="10" r="6" stroke="currentColor" stroke-width="1" opacity="0.5"/><circle cx="10" cy="10" r="8.5" stroke="currentColor" stroke-width="0.75" opacity="0.3"/></svg>
        </div>
        <div><div class="card-title">slack-index / minty-search</div><div class="card-sub"><span class="tag tag-polling">4h index</span> <span class="tag tag-persistent">Persistent bot</span> <span class="health-ok status-pulse">&#9679;</span></div></div>
      </div>
      <div class="card-body">
        <p>Two-part system. The <strong>indexer</strong> embeds all public Slack messages into ChromaDB every 4 hours with per-channel watermarks.</p>
        <p>The <strong>search bot</strong> (<code>@minty-search</code>) spawns a Claude Opus agent on @mention. It classifies intent, generates 3&ndash;5 diverse query variants, and synthesizes results into prose with inline Slack permalink citations.</p>
        <div class="detail-row"><span class="detail-label">Search strategy</span><span class="detail-val">Multi-query Reciprocal Rank Fusion (RRF, k=60). Agent generates diverse-angle queries, not synonyms. Scores fused across retrieval passes.</span></div>
        <div class="detail-row"><span class="detail-label">Resilience</span><span class="detail-val">3-tier: Primary agent &rarr; Relief officer (fresh context) &rarr; Direct search + formatter. Never returns a hard error.</span></div>
        <div class="detail-row"><span class="detail-label">UX</span><span class="detail-val">Haiku-generated contextual placeholder while Opus works. Whimsical progress messages after 2 min. Catch-up sweep on restart recovers missed queries.</span></div>
        <div class="detail-row"><span class="detail-label">Filters</span><span class="detail-val"><code>in:#channel</code>, <code>from:user</code>, <code>days:N</code>. Self-referential detection: "my posts" auto-maps to requesting user.</span></div>
        <div class="detail-row"><span class="detail-label">Index</span><span class="detail-val">ChromaDB at <code>data/slack_index/</code>, Voyage voyage-3 embeddings</span></div>
        <div class="detail-row"><span class="detail-label">News index</span><span class="detail-val">A separate news index (ChromaDB at <code>data/news_index/</code>) indexes <code>#mint-community</code> and <code>#firehose</code> for dedicated news search via <code>@Minty</code>'s News Search and Catch Me Up modes.</span></div>
        <div class="detail-row"><span class="detail-label">Key files</span><span class="detail-val"><code>index_slack.py</code>, <code>watcher.py</code>, <code>search_dispatch.py</code>, <code>agents/search_agent.md</code></span></div>
      </div>
    </div>

    <div class="card" data-search="cli-health-probe health auth monitor probe alert email">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(136,146,166,0.08);color:#8892a6">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><path d="M2 10h4l2-5 2 10 2-5h6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </div>
        <div><div class="card-title">cli-health-probe</div><div class="card-sub"><span class="tag tag-polling">Every 10m</span> <span class="health-ok">&#9679;</span></div></div>
      </div>
      <div class="card-body">
        <p>Minimal probe every 10 minutes through the daemon Claude CLI config. Detects auth failures, rate limits, and crashes. Emails administrators on failure and recovery.</p>
        <div class="detail-row"><span class="detail-label">Model</span><span class="detail-val">Claude Haiku (minimal probe call)</span></div>
        <div class="detail-row"><span class="detail-label">Alerts</span><span class="detail-val">Email to administrators on failure/recovery</span></div>
        <div class="detail-row"><span class="detail-label">Key files</span><span class="detail-val"><code>daemons/cli-health-probe/</code></span></div>
      </div>
    </div>

    <div class="card" data-search="ghost cms website newsletter mintresearch blog">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(136,146,166,0.08);color:#8892a6">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><circle cx="10" cy="10" r="7" stroke="currentColor" stroke-width="1.5"/><path d="M3 10h14M10 3c-2 2-3 4.5-3 7s1 5 3 7M10 3c2 2 3 4.5 3 7s-1 5-3 7" stroke="currentColor" stroke-width="1" opacity="0.5"/></svg>
        </div>
        <div><div class="card-title">ghost</div><div class="card-sub"><span class="tag tag-persistent">Persistent</span> <span class="health-ok status-pulse">&#9679;</span></div></div>
      </div>
      <div class="card-body">
        <p>Ghost CMS powering mintresearch.org. Local port 2368, exposed via Cloudflare Tunnel.</p>
        <div class="detail-row"><span class="detail-label">Model</span><span class="detail-val">None (web server)</span></div>
        <div class="detail-row"><span class="detail-label">URL</span><span class="detail-val">mintresearch.org / newsletter.mintresearch.org</span></div>
      </div>
    </div>

    <div class="card" data-search="subscribe-endpoint subscribe email newsletter http api endpoint">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(136,146,166,0.08);color:#8892a6">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><rect x="3" y="5" width="14" height="10" rx="1.5" stroke="currentColor" stroke-width="1.5"/><path d="M3 5l7 5 7-5" stroke="currentColor" stroke-width="1.5"/></svg>
        </div>
        <div><div class="card-title">subscribe-endpoint</div><div class="card-sub"><span class="tag tag-persistent">Persistent</span> <span class="health-ok status-pulse">&#9679;</span></div></div>
      </div>
      <div class="card-body">
        <p>HTTP server on <code>127.0.0.1:8099</code> for newsletter subscriptions. Validates, deduplicates, and appends to subscriber list. Public via Cloudflare Tunnel at <code>/api/subscribe</code>.</p>
        <div class="detail-row"><span class="detail-label">Model</span><span class="detail-val">None (HTTP endpoint)</span></div>
        <div class="detail-row"><span class="detail-label">Spam prevention</span><span class="detail-val">Honeypot field, rate limiting (5/min per IP), CORS restricted to <code>mint-philosophy.github.io</code></span></div>
        <div class="detail-row"><span class="detail-label">Key files</span><span class="detail-val"><code>daemons/subscribe-endpoint/subscribe_endpoint.py</code></span></div>
      </div>
    </div>

    <div class="card" data-search="guide-updater guide infrastructure html auto-sync">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(136,146,166,0.08);color:#8892a6">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><path d="M14 6A5 5 0 1 1 6 8" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/><path d="M14 2v4h-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </div>
        <div><div class="card-title">guide-updater</div><div class="card-sub"><span class="tag tag-daily">06:00 &amp; 18:00</span> <span class="health-ok">&#9679;</span></div></div>
      </div>
      <div class="card-body">
        <p>Auto-syncs this guide with the live codebase. Scans daemon configs, skill registries, and corpus stats, then patches the HTML. Twice daily.</p>
        <div class="detail-row"><span class="detail-label">Model</span><span class="detail-val">Claude Opus (diff analysis and HTML generation)</span></div>
        <div class="detail-row"><span class="detail-label">Schedule</span><span class="detail-val">Daily at 06:00 and 18:00 AEDT</span></div>
        <div class="detail-row"><span class="detail-label">Key files</span><span class="detail-val"><code>daemons/guide-updater/</code></span></div>
      </div>
    </div>

  </div>
</Section>

<!-- ============ TIMELINE ============ -->
<Section id="timeline" num="08">
  <h2 class="cli-call" data-section-name="Daemon Schedule"><span class="section-label">Daemon Schedule</span><span class="cli-line"><span class="fn">Bash</span><span class="op">(</span><span class="str">"launchctl list | grep minty"</span><span class="op">)</span><span class="cli-cursor"></span></span></h2>
  <p>When each daemon runs throughout the day (times in local/AEDT). Persistent daemons run continuously; polled daemons run at fixed intervals.</p>
  <div class="timeline-wrap">
    <div id="timelineChart"></div>
    <div class="timeline-hours" style="margin-left:142px">
      <span>0</span><span>2</span><span>4</span><span>6</span><span>8</span><span>10</span><span>12</span><span>14</span><span>16</span><span>18</span><span>20</span><span>22</span>
    </div>
  </div>
</Section>

<!-- ============ LAB MEMBERS ============ -->
<Section id="guide" num="09">
  <h2 class="cli-call" data-section-name="For Lab Members"><span class="section-label">For Lab Members</span><span class="cli-line"><span class="fn">AskUserQuestion</span><span class="op">(</span><span class="str">"What are we working on today?"</span><span class="op">)</span><span class="cli-cursor"></span></span></h2>
  <p class="lead">Everything runs in the background &mdash; you interact through Slack channels and bot mentions.</p>

  <details class="lab-accordion">
    <summary>What Happens Automatically</summary>
    <div class="accordion-inner">
      <ul class="clean">
        <li><strong>Daily digest</strong> &mdash; Each morning, a curated summary of yesterday's AI news arrives in your inbox and is posted as a canvas in <code>#mint-community</code> (Yesterday in AI). <a href="#subscribe">Subscribe here</a></li>
        <li><strong>Weekly roundup</strong> &mdash; Each Wednesday, a comprehensive weekly digest is posted as a canvas in <code>#lab-meetings</code> (Minty's Week in AI)</li>
        <li><strong>PDF attachment</strong> &mdash; When papers or articles are shared in <code>#mint-community</code>, PDFs are automatically found, downloaded, and posted as thread replies</li>
        <li><strong>Corpus ingestion</strong> &mdash; Papers uploaded to <code>#papers</code> are automatically processed into the searchable research corpus (embedding, 41-dimension analysis, Zotero cataloguing)</li>
      </ul>
    </div>
  </details>

  <details class="lab-accordion" id="bots">
    <summary>Using @Minty (Corpus Search)</summary>
    <div class="accordion-inner">
      <p>Available to everyone in <code>#mint-community</code>. Mention <code>@Minty</code> with a research question to get an interactive menu of 7 search modes &mdash; from instant Q&amp;A to 60-minute deep literature reviews posted as Slack canvases. See <a href="#corpus-search">&sect;05 @Minty</a> for full details.</p>
    </div>
  </details>

  <details class="lab-accordion">
    <summary>Using @minty-search (Slack Search)</summary>
    <div class="accordion-inner">
      <p>Full lab members only. Mention <code>@minty-search</code> with a natural-language query to search across all indexed Slack messages.</p>
      <ul class="clean">
        <li>Filter by channel: <code>@minty-search what did we discuss about alignment in:#papers</code></li>
        <li>Filter by person: <code>@minty-search from:seth posts about governance</code></li>
        <li>Filter by time: <code>@minty-search days:7 recent discussions on tool use</code></li>
        <li>Follow up in the same thread for refined searches</li>
      </ul>
    </div>
  </details>

  <details class="lab-accordion">
    <summary>Adding Papers to the Corpus</summary>
    <div class="accordion-inner">
      <div class="grid-2">
        <div>
          <h3 style="color:var(--mint);margin-top:0">One at a time</h3>
          <p>Upload a PDF directly to <code>#papers</code> in Slack. The ingestion pipeline processes it automatically &mdash; you'll see a check-mark reaction and a thread reply when it's done.</p>
        </div>
        <div>
          <h3 style="color:var(--amber);margin-top:0">Bulk uploads</h3>
          <p>Upload PDFs to the <strong>local-pdfs</strong> folder in Google Drive. They sync down and are processed overnight by the batch ingestion daemon (8 concurrent workers).</p>
        </div>
      </div>
      <div style="display:flex;gap:12px;flex-wrap:wrap;margin-top:12px">
        <a href="https://drive.google.com/drive/folders/1QjnK2zOQwgOgljj-ohUFt9YFdfuxGdJ3?usp=drive_link" target="_blank" class="badge badge-blue" style="font-size:12px;padding:6px 14px;text-decoration:none">Browse all lab PDFs</a>
        <a href="https://drive.google.com/drive/folders/1Kzj8mG-5BQCTk7ICu9nY-rWDytMxz4k1?usp=sharing" target="_blank" class="badge badge-green" style="font-size:12px;padding:6px 14px;text-decoration:none">Upload papers for corpus ingestion</a>
      </div>
    </div>
  </details>

  <details class="lab-accordion">
    <summary>How News Gets Curated</summary>
    <div class="accordion-inner">
      <p><strong>feedme-daily</strong> fetches hundreds of items each morning and classifies by relevance. Top 20&ndash;40% land in <code>#feed</code> where Seth thumbs-up the best. Promoted items flow to <code>#firehose</code>, then <strong>yesterday-in-ai</strong> produces a daily digest in <code>#mint-community</code>.</p>
    </div>
  </details>

  <details class="lab-accordion" id="slack-channels">
    <summary>Slack Channels</summary>
    <div class="accordion-inner">
      <p>The workspace has ~57 channels. Most day-to-day activity happens in a handful.</p>
      <RefTable>
        <thead><tr><th>Channel</th><th>What It's For</th></tr></thead>
        <tbody>
          <tr><td colspan="2" style="color:var(--mint);font-weight:600;padding-top:12px">Everyday</td></tr>
          <tr><td><code>#general</code></td><td>Announcements and all-hands discussion</td></tr>
          <tr><td><code>#random</code></td><td>Off-topic, jokes, misc</td></tr>
          <tr><td><code>#mint-community</code></td><td>AI news and discussion &mdash; daily digest, <code>@Minty</code> research Q&amp;A</td></tr>
          <tr><td><code>#firehose</code></td><td>Automated content hub &mdash; all daemon-posted content flows through here</td></tr>
          <tr><td colspan="2" style="color:var(--mint);font-weight:600;padding-top:12px">Research Projects</td></tr>
          <tr><td><code>#proj-*</code></td><td>One channel per active project. Post project-specific discussion, drafts, and updates.</td></tr>
          <tr><td colspan="2" style="color:var(--mint);font-weight:600;padding-top:12px">Reference &amp; Coordination</td></tr>
          <tr><td><code>#papers</code></td><td>Upload PDFs to add them to the research corpus (auto-ingested)</td></tr>
          <tr><td><code>#lab-meetings</code></td><td>Weekly digest canvas and meeting coordination</td></tr>
          <tr><td><code>#review-wip</code></td><td>Share works-in-progress for lab feedback</td></tr>
        </tbody>
      </RefTable>
    </div>
  </details>

  <details class="lab-accordion">
    <summary>Security</summary>
    <div class="accordion-inner">
      <p>Dedicated machine with credentials in macOS Keychain. No private data or credentials exposed to agents or lab members.</p>
    </div>
  </details>
</Section>

<!-- ============ AGENT ENGINEERING ============ -->
<Section id="agents" num="10">
  <h2 class="cli-call" data-section-name="Agent Engineering"><span class="section-label">Agent Engineering</span><span class="cli-line"><span class="fn">Agent</span><span class="op">(</span><span class="str">"spawn workers"</span><span class="op">,</span> <span class="kw">model</span><span class="op">=</span><span class="str">"opus"</span><span class="op">)</span><span class="cli-cursor"></span></span></h2>

  <h3>Worker Agent Types</h3>
  <div class="cards" style="grid-template-columns: repeat(auto-fill, minmax(280px, 1fr))">
    <div class="card" style="border-color:var(--mint)">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(46,196,182,0.08);color:var(--mint)">>_</div>
        <div><div class="card-title">generic-worker</div><div class="card-sub">Opus</div></div>
      </div>
      <div class="card-body">
        <div class="detail-row"><span class="detail-label">Tools</span><span class="detail-val">Read, Write, Edit, Bash, Glob, Grep, Web</span></div>
        <p>General-purpose delegated tasks. The default workhorse for anything that can be described in a prompt.</p>
      </div>
    </div>
    <div class="card" style="border-color:var(--amber)">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(245,158,11,0.08);color:var(--amber)">R</div>
        <div><div class="card-title">research-reader</div><div class="card-sub">Opus</div></div>
      </div>
      <div class="card-body">
        <div class="detail-row"><span class="detail-label">Tools</span><span class="detail-val">Read, Bash, Glob, Grep</span></div>
        <p>Deep academic paper reading with evidence-grounded analysis. Read-only access to workspace files.</p>
      </div>
    </div>
    <div class="card" style="border-color:var(--cyan)">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(34,211,238,0.08);color:var(--cyan)">41</div>
        <div><div class="card-title">corpus-worker</div><div class="card-sub">Opus</div></div>
      </div>
      <div class="card-body">
        <div class="detail-row"><span class="detail-label">Tools</span><span class="detail-val">Read, Write, Edit, Bash, Glob, Grep</span></div>
        <p>41-question semantic analysis + RAPTOR summaries. Used in batch processing via <code>/orchestrate</code>.</p>
      </div>
    </div>
    <div class="card" style="border-color:var(--indigo)">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(129,140,248,0.08);color:var(--indigo)">&Sigma;</div>
        <div><div class="card-title">raptor-worker</div><div class="card-sub">Opus</div></div>
      </div>
      <div class="card-body">
        <div class="detail-row"><span class="detail-label">Tools</span><span class="detail-val">Read, Write, Edit, Bash, Glob, Grep</span></div>
        <p>Hierarchical summary generation across paper clusters. Spawned in parallel via <code>/raptor</code>.</p>
      </div>
    </div>
    <div class="card" style="border-color:var(--purple)">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(167,139,250,0.08);color:var(--purple)">//</div>
        <div><div class="card-title">code-reviewer</div><div class="card-sub">Opus</div></div>
      </div>
      <div class="card-body">
        <div class="detail-row"><span class="detail-label">Tools</span><span class="detail-val">Read, Grep, Glob</span></div>
        <p>Code review against MINT Lab standards. Read-only &mdash; cannot modify files.</p>
      </div>
    </div>
  </div>

  <h3>Delegation Protocol</h3>
  <div class="callout"><p><strong>Mandatory:</strong> "Every tool call is context you burn. Subagents have fresh context. If a task can be described in a prompt, delegate it."</p></div>
  <ul class="clean">
    <li><span class="t-cyan">Web search</span>, <span class="t-green">documentation lookup</span>, <span class="t-yellow">file processing</span>, <span class="t-purple">batch operations</span> &rarr; always delegate</li>
    <li>Model policy: <strong class="t-accent">Opus</strong> for substantive work, <strong class="t-cyan">Sonnet</strong> for retrieval-only, <strong class="t-yellow">Haiku</strong> only for startup data gathering</li>
    <li>Hooks enforce this: <code>delegation-nudge.py</code> warns after 15 consecutive Read/Grep/Glob calls without a Task delegation</li>
  </ul>

  <h3>Skills &amp; Commands</h3>

  <details class="lab-accordion">
    <summary>Skills (16)</summary>
    <div class="accordion-inner">
      <p style="font-size:13px;color:var(--text-3);margin-bottom:12px">Reusable knowledge patterns in <code>.claude/skills/</code>. Each has a <code>SKILL.md</code> with triggers, scope, and detailed instructions.</p>

      <h4 style="color:var(--indigo);font-size:13px;margin:16px 0 8px">Research &amp; Corpus</h4>
      <RefTable>
        <thead><tr><th>Skill</th><th>Purpose</th></tr></thead>
        <tbody>
          <tr><td><code>paper-fetch</code></td><td>Download papers from 15+ publishers (3-tier cascade: requests &rarr; SeleniumBase &rarr; OpenClaw)</td></tr>
          <tr><td><code>codex-search</code></td><td>Web-aware research via OpenAI Codex CLI</td></tr>
        </tbody>
      </RefTable>

      <h4 style="color:var(--indigo);font-size:13px;margin:16px 0 8px">Communication</h4>
      <RefTable>
        <thead><tr><th>Skill</th><th>Purpose</th></tr></thead>
        <tbody>
          <tr><td><code>slack-posting</code></td><td>Post messages (bot token) and read DMs/files (user token)</td></tr>
          <tr><td><code>slack-search</code></td><td>Semantic search over indexed Slack messages via ChromaDB + RRF</td></tr>
          <tr><td><code>agent-email</code></td><td>Gmail API for the agent inbox</td></tr>
          <tr><td><code>twitter-fetch</code></td><td>Fetch tweet content via Twitter API v2 (OAuth1)</td></tr>
        </tbody>
      </RefTable>

      <h4 style="color:var(--indigo);font-size:13px;margin:16px 0 8px">Infrastructure</h4>
      <RefTable>
        <thead><tr><th>Skill</th><th>Purpose</th></tr></thead>
        <tbody>
          <tr><td><code>vault</code></td><td>Credential vault &mdash; macOS Keychain storage for all API keys and tokens</td></tr>
          <tr><td><code>github</code></td><td>GitHub repo management for <code>mint-philosophy</code> org</td></tr>
          <tr><td><code>workspace-search</code></td><td>Semantic search across entire workspace via ChromaDB</td></tr>
          <tr><td><code>codex-cli</code></td><td>OpenAI Codex CLI in headless exec mode</td></tr>
          <tr><td><code>systematic-debugging</code></td><td>4-phase root cause methodology (investigate before fixing)</td></tr>
          <tr><td><code>daemon-test</code></td><td>End-to-end integration tests for all Slack-connected daemons</td></tr>
          <tr><td><code>peer-review</code></td><td>Code review via Codex CLI (Gemini removed)</td></tr>
          <tr><td><code>openclaw-browser</code></td><td>Browser automation via OpenClaw CLI with persistent authentication. AI-optimized browser control for web scraping and interaction.</td></tr>
        </tbody>
      </RefTable>
    </div>
  </details>

  <details class="lab-accordion">
    <summary>Commands (24)</summary>
    <div class="accordion-inner">
      <p style="font-size:13px;color:var(--text-3);margin-bottom:12px">User-invocable slash commands in <code>.claude/commands/</code>. Invoke with <code>/name</code>.</p>

      <h4 style="color:var(--green);font-size:13px;margin:16px 0 8px">Session Lifecycle</h4>
      <RefTable>
        <thead><tr><th>Command</th><th>Purpose</th></tr></thead>
        <tbody>
          <tr><td><code>/start</code></td><td>Initialize session: git sync, UUID extraction, parallel subagent dispatch, briefing</td></tr>
          <tr><td><code>/end</code></td><td>Close session: reflection, delegate closure to subagent, git push</td></tr>
          <tr><td><code>/suspend</code></td><td>Pause session for later resumption</td></tr>
          <tr><td><code>/retro</code></td><td>Structured retrospective: friction scan, skill recognition, fact hygiene</td></tr>
          <tr><td><code>/name</code></td><td>Set display name for session tab</td></tr>
        </tbody>
      </RefTable>

      <h4 style="color:var(--green);font-size:13px;margin:16px 0 8px">Research Operations</h4>
      <RefTable>
        <thead><tr><th>Command</th><th>Purpose</th></tr></thead>
        <tbody>
          <tr><td><code>/corpus</code></td><td>Deep 41-question semantic paper analysis</td></tr>
          <tr><td><code>/orchestrate</code></td><td>Spawn 20 parallel corpus-workers for batch analysis</td></tr>
          <tr><td><code>/raptor</code></td><td>Spawn 20 parallel raptor-workers for summaries</td></tr>
          <tr><td><code>/research</code></td><td>Deep review pipeline over paper corpus (4-phase: search, readers, synthesis, QA)</td></tr>
          <tr><td><code>/metadata-extract</code></td><td>Batch metadata extraction from papers</td></tr>
        </tbody>
      </RefTable>

      <h4 style="color:var(--green);font-size:13px;margin:16px 0 8px">Content &amp; Admin</h4>
      <RefTable>
        <thead><tr><th>Command</th><th>Purpose</th></tr></thead>
        <tbody>
          <tr><td><code>/maintain</code></td><td>Infrastructure health check and repair (12 categories)</td></tr>
          <tr><td><code>/promote</code></td><td>Run feed promotion manually</td></tr>
          <tr><td><code>/peerreview</code></td><td>Code review via Codex CLI</td></tr>
          <tr><td><code>/screen</code></td><td>Capture and view current screen</td></tr>
        </tbody>
      </RefTable>
    </div>
  </details>

  <details class="lab-accordion">
    <summary>Hooks (8)</summary>
    <div class="accordion-inner">
      <p style="font-size:13px;color:var(--text-3);margin-bottom:12px">Python scripts that run before/after tool calls to enforce guardrails. 5 are wired in <code>settings.json</code>; 3 exist but are currently disabled.</p>
      <RefTable>
        <thead><tr><th>Hook</th><th>Trigger</th><th>Effect</th><th>Status</th></tr></thead>
        <tbody>
          <tr><td><code>block-rm.py</code></td><td>PreToolUse (Bash)</td><td><span class="badge badge-amber">Hard block</span> Prevents <code>rm</code>, <code>shred</code>, <code>unlink</code>. Forces <code>trash</code> CLI.</td><td><span class="health-ok">&#9679;</span></td></tr>
          <tr><td><code>block-web-direct.py</code></td><td>PreToolUse (Web*)</td><td><span class="badge badge-cyan">Soft nudge</span> Reminds agent to delegate web searches to subagents</td><td><span class="health-ok">&#9679;</span></td></tr>
          <tr><td><code>delegation-nudge.py</code></td><td>PreToolUse (Read/Grep/Glob)</td><td><span class="badge badge-cyan">Soft nudge</span> Warns after 15 consecutive file reads without delegation</td><td><span class="health-ok">&#9679;</span></td></tr>
          <tr><td><code>python-check.py</code></td><td>PostToolUse (Edit/Write)</td><td><span class="badge badge-indigo">Quality</span> Runs py_compile + Python 3.9 compat check on .py files</td><td><span class="health-ok">&#9679;</span></td></tr>
          <tr><td><code>statusline.py</code></td><td>StatusLine</td><td><span class="badge badge-green">Display</span> Context-aware status bar with color-coded usage</td><td><span class="health-ok">&#9679;</span></td></tr>
          <tr><td><code>bedtime.py</code></td><td>UserPromptSubmit</td><td><span class="badge badge-amber">Hard block</span> Blocks prompts between 23:55&ndash;06:00. "Go to bed, Seth."</td><td style="color:var(--text-3)">Unwired</td></tr>
          <tr><td><code>block-plan-mode.py</code></td><td>PreToolUse (EnterPlanMode)</td><td><span class="badge badge-amber">Hard block</span> Prevents plan mode (wipes context on exit)</td><td style="color:var(--text-3)">Unwired</td></tr>
          <tr><td><code>context-threshold.py</code></td><td>UserPromptSubmit</td><td><span class="badge badge-cyan">Soft nudge</span> Warns when context usage exceeds threshold (currently disabled internally)</td><td style="color:var(--text-3)">Unwired</td></tr>
        </tbody>
      </RefTable>
    </div>
  </details>

  <h3>Key Design Principles</h3>
  <ul class="clean">
    <li><strong class="t-cyan">Delegation-first:</strong> The main agent orchestrates; subagents do the work. "Every tool call is context you burn. Subagents have fresh context."</li>
    <li><strong class="t-green">Slack as message bus:</strong> All daemons communicate through Slack channels &mdash; no direct inter-daemon communication</li>
    <li><strong class="t-yellow">CLI subscriptions, not API:</strong> All LLM calls go through <code>claude -p</code> or Codex CLI &mdash; no metered API keys</li>
    <li><strong class="t-purple">Graceful degradation:</strong> Every pipeline stage can fail without stopping the pipeline. The system never returns empty-handed.</li>
  </ul>

  <h3>Session Identity</h3>
  <p style="font-size:13px">Each session is named <code>Minty-{'{'}hex{'}'}</code> where <code>{'{'}hex{'}'}</code> is the last 8 characters of the Claude Code session UUID (e.g. <code>Minty-4c4365b9</code>). Sessions are tracked in SQLite (<code>sessions.db</code>) with full-text search.</p>

  <h3>Memory Architecture</h3>
  <p>Cross-session continuity through layered memory:</p>
  <div class="cards" style="grid-template-columns: repeat(auto-fill, minmax(260px, 1fr))">
    <div class="card" style="border-color:var(--mint)">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(46,196,182,0.08);color:var(--mint)">{}</div>
        <div><div class="card-title">FACTS.md (Durable)</div><div class="card-sub">Slow-changing knowledge</div></div>
      </div>
      <div class="card-body"><p>Seth's preferences, API patterns, proven antipatterns, design decisions. Reviewed and pruned during <code>/retro</code>. 81KB.</p></div>
    </div>
    <div class="card" style="border-color:var(--amber)">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(245,158,11,0.08);color:var(--amber)">>></div>
        <div><div class="card-title">RECENT.md (Dynamic)</div><div class="card-sub">Fast-changing context</div></div>
      </div>
      <div class="card-body"><p>Active project threads, last 10 sessions, TODOs, blockers, someday list. Updated at every session close. 29KB.</p></div>
    </div>
    <div class="card" style="border-color:var(--cyan)">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(34,211,238,0.08);color:var(--cyan)">db</div>
        <div><div class="card-title">sessions.db (SQLite)</div><div class="card-sub">Full session history</div></div>
      </div>
      <div class="card-body"><p>Full session history with FTS5 search. 22 columns per session including log content. Portable via JSON exports in <code>sessions/records/</code>.</p></div>
    </div>
    <div class="card" style="border-color:var(--purple)">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(167,139,250,0.08);color:var(--purple)"><></div>
        <div><div class="card-title">Learning Loop (/retro)</div><div class="card-sub">Structured retrospective</div></div>
      </div>
      <div class="card-body"><p>8-phase retrospective: friction scan, pattern classification, skill recognition, fact hygiene. Lessons flow into FACTS.md, skills, or CLAUDE.md.</p></div>
    </div>
    <div class="card" style="border-color:var(--pink)">
      <div class="card-head" data-toggle-card="1">
        <div class="card-icon" style="background:rgba(244,114,182,0.08);color:var(--pink)">~></div>
        <div><div class="card-title">SpecStory (Cloud)</div><div class="card-sub">Full session transcripts</div></div>
      </div>
      <div class="card-body"><p>Complete session transcripts in SpecStory cloud. Semantic search across all past sessions for context recovery and audit.</p></div>
    </div>
  </div>

  <details class="lab-accordion">
    <summary>The Iron Laws</summary>
    <div class="accordion-inner">
      <p style="font-size:13px;color:var(--text-3);margin-bottom:12px">Foundational rules that override all other behavior.</p>
      <RefTable>
        <thead><tr><th>Law</th><th>Rule</th></tr></thead>
        <tbody>
          <tr><td><strong>0 &mdash; No Shortcuts</strong></td><td>When asked for "all" &mdash; do ALL. Cherry-picking = failure.</td></tr>
          <tr><td><strong>1 &mdash; Evidence Over Claims</strong></td><td>"It should work" = failure. Verify now, not later.</td></tr>
          <tr><td><strong>2 &mdash; Document Everything</strong></td><td>If not written down, didn't happen.</td></tr>
          <tr><td><strong>3 &mdash; Persistence</strong></td><td>Try 3+ approaches before asking. Solve, don't report.</td></tr>
          <tr><td><strong>5 &mdash; Complete the Checklist</strong></td><td>"Partially done" = not done.</td></tr>
          <tr><td><strong>6 &mdash; Think Before Acting</strong></td><td>Never rm &mdash; use trash. Map dependencies first.</td></tr>
        </tbody>
      </RefTable>
    </div>
  </details>

  <h3>Model Policy</h3>
  <p style="font-size:13px">All LLM usage goes through CLI subscriptions (<code>claude -p</code>, Codex CLI) — not metered API calls. Generous subscriptions mean no rate limit concerns.</p>
  <RefTable>
    <thead><tr><th>Tier</th><th>Model</th><th>Use Case</th></tr></thead>
    <tbody>
      <tr><td><span class="badge badge-indigo">Default</span></td><td>Claude Opus</td><td>All substantive work: analysis, classification, code generation, writing, debugging</td></tr>
      <tr><td><span class="badge badge-cyan">Retrieval</span></td><td>Claude Sonnet 4.6</td><td>Retrieval-only tasks: fetching data, reading files, checking status, simple lookups</td></tr>
      <tr><td><span class="badge badge-green">Review / Analysis</span></td><td>Codex gpt-5.2 / gpt-5.3-codex-spark</td><td>Code review, corpus analysis, enrichment, fact-checking, and link verification. Spark used for verification and enrichment tasks.</td></tr>
      <tr><td><span class="badge badge-amber">Startup</span></td><td>Claude Haiku</td><td>Lightweight data-gathering subagents during /start Phase 2 only</td></tr>
    </tbody>
  </RefTable>
</Section>

<!-- ============ INTEGRATIONS ============ -->
<Section id="integrations" num="11">
  <h2 class="cli-call" data-section-name="External Integrations"><span class="section-label">External Integrations</span><span class="cli-line"><span class="fn">WebFetch</span><span class="op">(</span><span class="kw">url</span><span class="op">=</span><span class="str">"https://api.*"</span><span class="op">)</span><span class="cli-cursor"></span></span></h2>
  <p>The system connects to 16 external services across five categories.</p>

  <h3 style="margin-top:20px">LLM &amp; Embedding</h3>
  <div class="cards" style="grid-template-columns: repeat(auto-fill, minmax(240px, 1fr))">
    <div class="card" style="border-color:var(--indigo)" data-ingest-tip="Primary model for all substantive work. Powers classification (16 parallel workers), corpus analysis (41 semantic questions), daily digest generation, deep literature reviews, and all agent reasoning. Used via CLI subscription with per-account daily rate limits." data-ingest-color="#818cf8">
      <div class="card-head"><div><div class="card-title">Claude Opus</div><div class="card-sub">Default model for all substantive work: analysis, classification, code generation, writing. Via CLI subscription.</div></div></div>
    </div>
    <div class="card" style="border-color:var(--indigo)" data-ingest-tip="Used for retrieval-only tasks that don't require deep reasoning: fetching data, reading and reporting file contents, searching indexes, checking daemon health, status collection. Reduces compute costs without sacrificing quality on mechanical tasks." data-ingest-color="#818cf8">
      <div class="card-head"><div><div class="card-title">Claude Sonnet 4.6</div><div class="card-sub">Retrieval-only tasks: fetching data, reading files, status checks. Via CLI subscription.</div></div></div>
    </div>
    <div class="card" style="border-color:var(--indigo)" data-ingest-tip="OpenAI models used as secondary LLMs. GPT-5.2 handles code review, corpus analysis enrichment, fact-checking, and link verification in the deep review pipeline. Spark (lightweight variant) for cheaper tasks like markdown cleanup." data-ingest-color="#818cf8">
      <div class="card-head"><div><div class="card-title">Codex GPT-5.2 / Spark</div><div class="card-sub">Code review, corpus analysis, enrichment, fact-checking, link verification. Spark for lighter tasks.</div></div></div>
    </div>
    <div class="card" style="border-color:var(--indigo)" data-ingest-tip="Voyage AI embedding models. context-3 produces 2048-dimensional vectors for the paper corpus (48 separate embeddings per document across semantic dimensions). voyage-3 produces 1024d vectors for Slack message and workspace file indexing." data-ingest-color="#818cf8">
      <div class="card-head"><div><div class="card-title">Voyage context-3 / voyage-3</div><div class="card-sub">Embedding models: context-3 (2048d) for paper corpus, voyage-3 (1024d) for Slack messages.</div></div></div>
    </div>
  </div>

  <h3>Communication</h3>
  <div class="cards" style="grid-template-columns: repeat(auto-fill, minmax(240px, 1fr))">
    <div class="card" style="border-color:var(--green)" data-ingest-tip="Central communication hub. Three bot tokens (Minty for corpus search, minty-search for indexing, feedme for content pipeline). Socket Mode for real-time events (messages, reactions, mentions), Web API for posting. 6+ monitored channels including #firehose, #mint-community, #minty-inbox." data-ingest-color="#4ade80">
      <div class="card-head"><div><div class="card-title">Slack</div><div class="card-sub">3 bots (Minty, minty-search, feedme), 6+ monitored channels. Socket Mode for real-time, Web API for posting.</div></div></div>
    </div>
    <div class="card" style="border-color:var(--green)" data-ingest-tip="Two accounts: work (seth@mintresearch.org) for interactive sessions, personal (sethlazar@gmail.com) for daemon operations. OAuth2 with separate client credentials. Handles daily digest delivery to 29 lab members and automated agent correspondence." data-ingest-color="#4ade80">
      <div class="card-head"><div><div class="card-title">Gmail</div><div class="card-sub">Daily digest delivery to 29 lab members. Agent inbox for automated correspondence.</div></div></div>
    </div>
    <div class="card" style="border-color:var(--green)" data-ingest-tip="Self-hosted Ghost CMS instance powering mintresearch.org. Hosts the lab website, blog, and email newsletter platform. Runs locally on port 2368, exposed to the internet via Cloudflare Tunnel. Yesterday in AI posts are published here daily." data-ingest-color="#4ade80">
      <div class="card-head"><div><div class="card-title">Ghost CMS</div><div class="card-sub">newsletter.mintresearch.org &mdash; blog and email newsletter platform via Cloudflare Tunnel.</div></div></div>
    </div>
  </div>

  <h3>Academic Sources</h3>
  <div class="cards" style="grid-template-columns: repeat(auto-fill, minmax(240px, 1fr))">
    <div class="card" style="border-color:var(--amber)" data-ingest-tip="Primary source for preprints. The feedme-daily daemon fetches new papers from tracked categories using the arXiv API with a 5-day rolling window (to handle weekend gaps). Direct PDF download via /abs/ to /pdf/ URL transform. Also provides HTML full-text for newer papers." data-ingest-color="#fbbf24">
      <div class="card-head"><div><div class="card-title">arXiv</div><div class="card-sub">Daily paper fetch (last 5 days window). Direct PDF + HTML access.</div></div></div>
    </div>
    <div class="card" style="border-color:var(--amber)" data-ingest-tip="Three complementary APIs for metadata enrichment. Semantic Scholar provides citation counts and related papers. CrossRef resolves DOIs to full bibliographic records. Unpaywall finds open-access PDF versions of paywalled papers via DOI lookup." data-ingest-color="#fbbf24">
      <div class="card-head"><div><div class="card-title">Semantic Scholar / CrossRef / Unpaywall</div><div class="card-sub">Metadata enrichment, citation data, open-access PDF resolution.</div></div></div>
    </div>
    <div class="card" style="border-color:var(--amber)" data-ingest-tip="Reference manager integration via Zotero API. Papers are automatically catalogued after corpus ingestion: title, authors, year, abstract, and tags derived from cluster labels. 1,237 items catalogued (817 added, 420 pre-existing)." data-ingest-color="#fbbf24">
      <div class="card-head"><div><div class="card-title">Zotero</div><div class="card-sub">Bibliographic cataloging. Papers uploaded after ingestion.</div></div></div>
    </div>
    <div class="card" style="border-color:var(--amber)" data-ingest-tip="Philosophy-specific academic database. Used for discovering papers in normative ethics, political philosophy, philosophy of AI, and related areas that may not appear on arXiv. Periodic scraping for new entries in tracked categories." data-ingest-color="#fbbf24">
      <div class="card-head"><div><div class="card-title">PhilPapers</div><div class="card-sub">Philosophy-specific paper discovery and metadata.</div></div></div>
    </div>
  </div>

  <h3>Content &amp; Social Feeds</h3>
  <div class="cards" style="grid-template-columns: repeat(auto-fill, minmax(240px, 1fr))">
    <div class="card" style="border-color:var(--red)" data-ingest-tip="OAuth1 authentication via API v2. Monitors curated lists and timelines for AI/ML researchers, labs, and policy organisations. The feedme-daily daemon fetches tweets, resolves t.co shortlinks, and expands threads. news-pdf-bot uses the API for thread expansion (Tier 2 PDF acquisition)." data-ingest-color="#f87171">
      <div class="card-head"><div><div class="card-title" style="color:var(--red)">Twitter/X API v2</div><div class="card-sub">OAuth1 auth. Timeline and list monitoring for AI/ML content.</div></div></div>
    </div>
    <div class="card" style="border-color:var(--red)" data-ingest-tip="AT Protocol firehose connection monitoring AI safety and governance discussions on Bluesky. Fetches posts from tracked feeds and user lists. Integrated into the feedme-daily pipeline alongside Twitter and RSS sources." data-ingest-color="#f87171">
      <div class="card-head"><div><div class="card-title" style="color:var(--red)">Bluesky</div><div class="card-sub">Firehose monitoring for AI safety and governance discussions.</div></div></div>
    </div>
    <div class="card" style="border-color:var(--red)" data-ingest-tip="30+ RSS and Substack feeds tracked via feedparser. Sources include Alignment Forum, LessWrong, AI safety newsletters, policy blogs, and selected Substack authors. Full article content extracted where available; abstracts used as fallback." data-ingest-color="#f87171">
      <div class="card-head"><div><div class="card-title" style="color:var(--red)">RSS / Substack</div><div class="card-sub">30+ feeds including Alignment Forum, LessWrong, AI newsletters.</div></div></div>
    </div>
  </div>

  <h3>Infrastructure</h3>
  <div class="cards" style="grid-template-columns: repeat(auto-fill, minmax(240px, 1fr))">
    <div class="card" style="border-color:var(--cyan)" data-ingest-tip="Bidirectional sync via rclone daemon (every 10 minutes). PDFs uploaded after corpus ingestion with canonical naming. Markdown conversions stored alongside originals. Shared lab folders for collaborative access. Also serves as the backing store for paper-map click-through links." data-ingest-color="#22d3ee">
      <div class="card-head"><div><div class="card-title">Google Drive</div><div class="card-sub">Bidirectional sync via rclone. PDF upload, markdown storage, shared lab folders.</div></div></div>
    </div>
    <div class="card" style="border-color:var(--cyan)" data-ingest-tip="mint-philosophy GitHub organisation. 12 repositories including minty (public agent workspace), corpus-search (retrieval infrastructure), yesterday-in-ai (digest pipeline), paper-map (corpus visualisation), and mint-lab-guide (this site). Automated via PAT for push operations." data-ingest-color="#22d3ee">
      <div class="card-head"><div><div class="card-title">GitHub</div><div class="card-sub">mint-philosophy org. 12 repos including minty, corpus-search, yesterday-in-ai.</div></div></div>
    </div>
  </div>
</Section>

<!-- ============ SUBSCRIBE ============ -->
<section id="subscribe" class="no-brackets" style="text-align:center;padding:48px;max-width:700px">
  <span class="section-num" style="opacity:0.3">&#10022;</span>
  <h2 style="justify-content:center">Yesterday in AI</h2>
  <p style="color:var(--text-2);margin-bottom:20px">Daily AI news digest from the MINT Lab. One email per weekday morning.</p>
  <div style="background:var(--bg-0);border-radius:0;padding:20px 24px;margin:20px 0;text-align:left;font-size:13.5px;line-height:1.7;border:1px solid var(--border);color:var(--text-2)">
    <p style="margin:0 0 12px 0"><span style="color:var(--text-1);font-weight:600">Yesterday in AI</span> lands each weekday morning. Covers AI safety, ethics, governance, and capabilities as narrative prose &mdash; contextual journalism, not punditry.</p>
    <p style="margin:0 0 12px 0"><span style="color:var(--text-1);font-weight:600">Minty's Week in AI</span> ships each Wednesday in <span style="font-family:var(--font);font-size:12px;color:var(--text-3)">#lab-meetings</span> &mdash; thematic sections covering the week's developments for lab meeting prep.</p>
    <p style="margin:0;color:var(--text-3);font-size:12.5px">Both assembled from the lab's Slack content pipeline and synthesized by Claude Opus.</p>
  </div>
  <form id="subscribe-form" style="display:flex;gap:10px;justify-content:center;flex-wrap:wrap;margin-top:20px">
    <input type="email" name="email" required placeholder="you@university.edu" style="padding:10px 16px;border-radius:0;border:1px solid var(--border);background:var(--bg-0);color:var(--text-1);font-size:14px;font-family:var(--font);width:280px;outline:none;transition:border-color 0.2s" onfocus="this.style.borderColor='var(--mint)'" onblur="this.style.borderColor='var(--border)'">
    <div style="position:absolute;left:-9999px" aria-hidden="true"><input type="text" name="website" tabindex="-1" autocomplete="off"></div>
    <button type="submit" style="padding:10px 24px;border-radius:0;border:none;background:var(--mint);color:#06080c;font-size:14px;font-family:var(--font);font-weight:600;cursor:pointer;transition:opacity 0.2s" onmouseover="this.style.opacity='0.85'" onmouseout="this.style.opacity='1'">Subscribe</button>
  </form>
  <p id="subscribe-msg" style="margin-top:14px;font-size:14px;min-height:20px"></p>
</section>

<!-- ============ FOOTER ============ -->
<section class="no-brackets" style="text-align:center;padding:48px;color:var(--text-3);font-size:12px;font-family:var(--font);letter-spacing:0.5px">
  <p>MINT Lab &mdash; Machine Intelligence &amp; Normative Theory &mdash; ANU and Johns Hopkins University</p>
  <p style="margin-top:4px">Updated March 2026</p>
</section>

<!-- Token counter (Claude Code output indicator homage) -->
<div class="token-counter" id="tokenCounter">
  <span class="arrow">↓</span> <span id="tokenCount">0</span> tokens
</div>

<!-- Section indicator bar (shows current section while scrolling) -->
<div class="section-indicator" id="sectionIndicator">
  <span class="si-num" id="siNum"></span>
  <span class="si-name" id="siName"></span>
  <span class="si-cli" id="siCli"></span>
</div>

</main>

<script is:inline>
// ===== Shared tooltip positioning helper =====
function clampTip(tip, anchorRect, containerSel) {
  var pad = 10;
  var tw = tip.offsetWidth || 300;
  var th = tip.offsetHeight || 80;
  var vw = window.innerWidth;
  var vh = window.innerHeight;
  // Get container bounds (diagram or section) to avoid going outside it
  var container = anchorRect._container;
  if (!container && containerSel) {
    var el = document.querySelector(containerSel);
    if (el) container = el.getBoundingClientRect();
  }
  var minX = container ? Math.max(pad, container.left) : pad;
  var maxX = container ? Math.min(vw - pad, container.right) : vw - pad;
  // Position below the anchor, centered horizontally
  var x = anchorRect.left + (anchorRect.width / 2) - (tw / 2);
  var y = anchorRect.bottom + 8;
  // Clamp horizontal: stay within container/viewport
  if (x + tw > maxX) x = maxX - tw;
  if (x < minX) x = minX;
  // If it goes below viewport, flip above
  if (y + th > vh) y = anchorRect.top - th - 8;
  if (y < pad) y = pad;
  tip.style.left = x + 'px';
  tip.style.top = y + 'px';
}

// ===== Make a tooltip draggable on touch =====
function makeDraggable(tip) {
  var dragging = false, startX, startY, origX, origY;
  tip.addEventListener('touchstart', function(e) {
    if (e.touches.length !== 1) return;
    dragging = true;
    startX = e.touches[0].clientX;
    startY = e.touches[0].clientY;
    origX = parseInt(tip.style.left) || 0;
    origY = parseInt(tip.style.top) || 0;
    tip.style.transition = 'none';
    e.preventDefault();
  }, {passive: false});
  tip.addEventListener('touchmove', function(e) {
    if (!dragging) return;
    var dx = e.touches[0].clientX - startX;
    var dy = e.touches[0].clientY - startY;
    tip.style.left = (origX + dx) + 'px';
    tip.style.top = (origY + dy) + 'px';
    e.preventDefault();
  }, {passive: false});
  tip.addEventListener('touchend', function() {
    dragging = false;
    tip.style.transition = '';
  });
}

// ===== Pipeline tooltips =====
(function() {
  var tip = document.getElementById('pipelineTip');
  if (!tip) return;
  var isTouch = 'ontouchstart' in window;
  var activeNode = null;
  var pipeWrap = document.querySelector('.pipeline-wrap');
  makeDraggable(tip);

  document.querySelectorAll('.pn').forEach(function(node) {
    if (!isTouch) {
      node.addEventListener('mouseenter', function(e) {
        var text = this.getAttribute('data-tip');
        if (!text) return;
        tip.textContent = text;
        var rect = this.querySelector('rect');
        var col = rect ? rect.getAttribute('stroke') : null;
        tip.style.borderColor = (col && col !== 'rgba(255,255,255,0.1)') ? col : 'var(--mint)';
        tip.classList.add('visible');
      });
      node.addEventListener('mousemove', function(e) {
        var x = e.clientX + 14;
        var y = e.clientY + 14;
        var vw = window.innerWidth;
        var pw = pipeWrap ? pipeWrap.getBoundingClientRect() : null;
        var maxR = pw ? pw.right : vw;
        var minL = pw ? pw.left : 0;
        if (x + 330 > maxR) x = maxR - 330;
        if (x < minL) x = minL;
        if (y + 100 > window.innerHeight) y = e.clientY - 80;
        tip.style.left = x + 'px';
        tip.style.top = y + 'px';
      });
      node.addEventListener('mouseleave', function() {
        tip.classList.remove('visible');
      });
    }

    // Touch: tap to toggle
    if (isTouch) {
      node.addEventListener('click', function(e) {
        e.preventDefault();
        e.stopPropagation();
        if (activeNode === this && tip.classList.contains('visible')) {
          tip.classList.remove('visible');
          activeNode = null;
          return;
        }
        var text = this.getAttribute('data-tip');
        if (!text) return;
        tip.textContent = text;
        var rect = this.querySelector('rect');
        var col = rect ? rect.getAttribute('stroke') : null;
        tip.style.borderColor = (col && col !== 'rgba(255,255,255,0.1)') ? col : 'var(--mint)';
        // Position clamped to pipeline diagram bounds
        var svgEl = this.closest('svg');
        var svgRect = svgEl ? svgEl.getBoundingClientRect() : null;
        var nodeRect = this.getBoundingClientRect();
        var ar = {left: nodeRect.left, right: nodeRect.right, top: nodeRect.top, bottom: nodeRect.bottom, width: nodeRect.width, height: nodeRect.height};
        ar._container = svgRect;
        tip.classList.add('visible');
        // Need to measure after visible so offsetWidth/Height are real
        requestAnimationFrame(function() { clampTip(tip, ar); });
        activeNode = this;
      });
    }
  });

  if (isTouch) {
    document.addEventListener('click', function(e) {
      if (activeNode && !activeNode.contains(e.target) && !tip.contains(e.target)) {
        tip.classList.remove('visible');
        activeNode = null;
      }
    });
  }
})();

// ===== Hover/tap tooltips (ingest stages, corpus search cards, etc.) =====
(function() {
  var tip = document.getElementById('ingestTip');
  if (!tip) return;
  var isTouch = 'ontouchstart' in window;
  var activeEl = null;
  makeDraggable(tip);

  document.querySelectorAll('[data-ingest-tip]').forEach(function(el) {
    if (!isTouch) {
      el.addEventListener('mouseenter', function(e) {
        tip.innerHTML = this.getAttribute('data-ingest-tip');
        tip.style.borderColor = this.getAttribute('data-ingest-color') || 'var(--mint)';
        tip.classList.add('visible');
        activeEl = this;
      });
      el.addEventListener('mousemove', function(e) {
        var x = e.clientX + 14;
        var y = e.clientY + 14;
        if (x + 350 > window.innerWidth) x = window.innerWidth - 360;
        if (x < 10) x = 10;
        if (y + 100 > window.innerHeight) y = e.clientY - 80;
        tip.style.left = x + 'px';
        tip.style.top = y + 'px';
      });
      el.addEventListener('mouseleave', function() {
        tip.classList.remove('visible');
        activeEl = null;
      });
    }

    if (isTouch) {
      el.addEventListener('click', function(e) {
        e.preventDefault();
        e.stopPropagation();
        if (activeEl === this && tip.classList.contains('visible')) {
          tip.classList.remove('visible');
          activeEl = null;
          return;
        }
        tip.innerHTML = this.getAttribute('data-ingest-tip');
        tip.style.borderColor = this.getAttribute('data-ingest-color') || 'var(--mint)';
        // Clamp within nearest section or viewport
        var elRect = this.getBoundingClientRect();
        var section = this.closest('section');
        var ar = {left: elRect.left, right: elRect.right, top: elRect.top, bottom: elRect.bottom, width: elRect.width, height: elRect.height};
        ar._container = section ? section.getBoundingClientRect() : null;
        tip.classList.add('visible');
        requestAnimationFrame(function() { clampTip(tip, ar); });
        activeEl = this;
      });
    }
  });

  if (isTouch) {
    document.addEventListener('click', function(e) {
      if (activeEl && !activeEl.contains(e.target) && !tip.contains(e.target)) {
        tip.classList.remove('visible');
        activeEl = null;
      }
    });
  }
})();

// Mobile menu is handled by BaseLayout.astro — no duplicate listeners here.

// Card toggle is handled by BaseLayout.astro (line 155-158) which attaches
// click listeners to all .card .card-head elements. No duplicate here.

// ===== Accordion toggle =====
function toggleAcc(el) {
  el.classList.toggle('open');
}

// ===== Tabs =====
function switchTab(e, id) {
  document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
  document.querySelectorAll('.tab-pane').forEach(p => p.classList.remove('active'));
  e.target.classList.add('active');
  document.getElementById(id).classList.add('active');
}

// ===== Search =====
const searchData = [
  {t:'feedme-daily',d:'Daily content curation: fetch, classify, rank from 7 sources',tag:'Daemon',c:'#38bdf8',s:'#daemons'},
  {t:'feed-promote',d:'Copy thumbsed-up items from #feed to #firehose',tag:'Daemon',c:'#4ade80',s:'#daemons'},
  {t:'share-to-minty',d:'Real-time URL ingestion from any device via Slack',tag:'Daemon',c:'#fb923c',s:'#daemons'},
  {t:'news-pdf-bot',d:'Find PDFs and generate markdown for paper URLs in #mint-community',tag:'Daemon',c:'#a78bfa',s:'#daemons'},
  {t:'yesterday-in-ai',d:'Daily AI news digest to 29 lab members across 11 institutions',tag:'Daemon',c:'#f472b6',s:'#daemons'},
  {t:'last-week-in-ai',d:'Weekly AI digest with multi-agent sectioned pipeline',tag:'Daemon',c:'#f472b6',s:'#daemons'},
  {t:'corpus-agent (@Minty)',d:'Slack bot: 7 modes (Fast Search, Fast Review, Deep Review, Conversation, List Previous, News, Catch Me Up), ~2,200-paper corpus',tag:'Daemon',c:'#38bdf8',s:'#corpus-search'},
  {t:'corpus-ingest-local',d:'Overnight batch processor: 8 workers, 10-stage pipeline, active 20:15-07:45 AEDT',tag:'Daemon',c:'#2dd4bf',s:'#daemons'},
  {t:'corpus-ingest-slack',d:'Daytime real-time Slack ingestion: 2 workers, active 08:00-20:00 AEDT',tag:'Daemon',c:'#2dd4bf',s:'#daemons'},
  {t:'corpus-ingest-writer',d:'Serialized LanceDB writer: eliminates concurrent write conflicts, always on',tag:'Daemon',c:'#2dd4bf',s:'#daemons'},
  {t:'cli-health-probe',d:'Sentinel daemon: probes CLI auth every 10m, emails alerts on failure/recovery',tag:'Daemon',c:'#94a3b8',s:'#daemons'},
  {t:'minty-search',d:'Agentic Slack search: Opus agent with multi-query RRF fusion, 3-tier resilience, contextual UX',tag:'Daemon',c:'#818cf8',s:'#daemons'},
  {t:'workspace-index',d:'Semantic workspace file search via ChromaDB',tag:'Daemon',c:'#818cf8',s:'#daemons'},
  {t:'rclone-sync',d:'Google Drive bisync + daily corpus backups',tag:'Daemon',c:'#2dd4bf',s:'#daemons'},
  {t:'vault.py',d:'Credential vault — macOS Keychain storage for API keys and tokens',tag:'Infra',c:'#f87171',s:'#agents'},
  {t:'CLAUDE.md',d:'Master agent instructions, Iron Laws, strategies',tag:'Config',c:'#fbbf24',s:'#overview'},
  {t:'session_db.py',d:'SQLite session tracking with FTS5 search',tag:'Script',c:'#fb923c',s:'#agents'},
  {t:'platform_config.py',d:'Cross-platform paths, secrets, env helpers for daemons',tag:'Script',c:'#fb923c',s:'#agents'},
  {t:'LanceDB',d:'Vector DB: ~2,200 papers, ~155K chunks, 29 topic clusters, ~4.5 GB',tag:'Database',c:'#2dd4bf',s:'#corpus'},
  {t:'ChromaDB (Slack)',d:'Indexed Slack messages, refreshed every 4 hours',tag:'Database',c:'#818cf8',s:'#agents'},
  {t:'ChromaDB (Workspace)',d:'Indexed workspace files, refreshed daily',tag:'Database',c:'#818cf8',s:'#agents'},
  {t:'/start',d:'Initialize session with git sync, UUID, parallel briefing',tag:'Command',c:'#4ade80',s:'#agents'},
  {t:'/end',d:'Close session: reflection, delegate closure, git push',tag:'Command',c:'#4ade80',s:'#agents'},
  {t:'/corpus',d:'Deep 41-question semantic paper analysis',tag:'Command',c:'#4ade80',s:'#agents'},
  {t:'/orchestrate',d:'Spawn 20 parallel corpus-workers for batch analysis',tag:'Command',c:'#4ade80',s:'#agents'},
  {t:'/research',d:'Deep review pipeline over paper corpus (4-phase: search, readers, synthesis, QA)',tag:'Command',c:'#4ade80',s:'#agents'},
  {t:'/maintain',d:'Infrastructure health check across 12 categories',tag:'Command',c:'#4ade80',s:'#agents'},
  {t:'paper-fetch',d:'Download papers from 15+ publishers via 3-tier cascade',tag:'Skill',c:'#818cf8',s:'#agents'},
  {t:'Deep Review Pipeline',d:'4-phase: Multi-round search (Opus) -> Parallel readers (Codex GPT-5.2 xhigh + medium) -> Synthesis (Opus) -> QA verification (Opus)',tag:'Architecture',c:'#fbbf24',s:'#agents'},
  {t:'Corpus Tools',d:'Search, paper retrieval, analysis, RAPTOR summaries, similarity, topics, dimensions + GitHub repo exploration',tag:'Architecture',c:'#38bdf8',s:'#agents'},
  {t:'Parallel Reading',d:'Papers split into top-tier (Codex xhigh, ~3 papers, 6 readers) and summary-tier (Codex medium, batches of 20) -- all readers run in parallel',tag:'Architecture',c:'#fbbf24',s:'#agents'},
  {t:'Ingest Pipeline',d:'3-daemon system: ingest-local (8 workers overnight) + ingest-slack (2 workers daytime) + writer (serialized DB). 11-stage per-paper pipeline.',tag:'Architecture',c:'#2dd4bf',s:'#corpus'},
  {t:'41-Question Analysis',d:'Semantic analysis across research core, methodology, AI-specific, meta, advanced',tag:'Corpus',c:'#2dd4bf',s:'#corpus'},
  {t:'RAPTOR Summaries',d:'Hierarchical summaries: document + section level per paper',tag:'Corpus',c:'#2dd4bf',s:'#corpus'},
  {t:'Iron Laws',d:'Foundational rules: No Shortcuts, Evidence Over Claims, Persistence, etc.',tag:'Policy',c:'#f87171',s:'#agents'},
  {t:'block-rm.py',d:'Hook: hard-blocks rm/shred/unlink, forces trash CLI',tag:'Hook',c:'#fb923c',s:'#agents'},
  {t:'bedtime.py',d:'Hook: blocks prompts 23:55-06:00. "Go to bed, Seth."',tag:'Hook',c:'#fb923c',s:'#agents'},
  {t:'Slack #firehose',d:'Automated hub channel: 2 daemons write, 3 daemons read',tag:'Channel',c:'#fbbf24',s:'#pipeline'},
  {t:'Slack #mint-community',d:'Human discussion channel: daily digest, lab Q&A, news sharing',tag:'Channel',c:'#4ade80',s:'#pipeline'},
  {t:'generic-worker',d:'General-purpose subagent for delegated tasks (Opus)',tag:'Agent',c:'#fb923c',s:'#agents'},
  {t:'corpus-worker',d:'41-question analysis + RAPTOR summaries per paper (Opus)',tag:'Agent',c:'#fb923c',s:'#agents'},
  {t:'research-reader',d:'Deep paper reading with evidence-grounded analysis (Opus)',tag:'Agent',c:'#fb923c',s:'#agents'},
  {t:'openclaw-browser',d:'Browser automation via OpenClaw CLI with persistent authentication',tag:'Skill',c:'#818cf8',s:'#agents'},
  {t:'OpenAI Codex CLI',d:'LLM backend for analysis, verification, and agentic search in corpus-ingest, yesterday-in-ai, slack-search',tag:'Integration',c:'#fbbf24',s:'#integrations'},
  {t:'archive-feed-channels',d:'Archives stale #feed-{day} channels after LWAI consumes them (Wed 12:00)',tag:'Daemon',c:'#fbbf24',s:'#daemons'},
  {t:'corpus-ingest-handoff',d:'Moves unprocessed Slack PDFs to local queue for overnight batch processing (daily 20:15)',tag:'Daemon',c:'#2dd4bf',s:'#daemons'},
  {t:'ghost',d:'Ghost CMS for mintresearch.org — website + newsletter platform',tag:'Daemon',c:'#94a3b8',s:'#daemons'},
  {t:'cloudflared',d:'Cloudflare Tunnel routing mintresearch.org -> localhost:2368',tag:'Daemon',c:'#94a3b8',s:'#daemons'},
  {t:'guide-updater',d:'Auto-syncs infrastructure guide HTML with live codebase, runs 06:00 and 18:00',tag:'Daemon',c:'#94a3b8',s:'#daemons'},
  {t:'ChromaDB (News)',d:'News messages from #mint-community and #firehose, refreshed hourly',tag:'Database',c:'#818cf8',s:'#agents'},
  {t:'Slack #firehose',d:'Automated content hub -- 2 daemons write, 3 daemons read, raw feed access',tag:'Channel',c:'#fbbf24',s:'#guide'},
];

function openSearch() {
  document.getElementById('searchOverlay').classList.add('open');
  document.getElementById('searchInput').value = '';
  document.getElementById('searchInput').focus();
  doSearch('');
}
function closeSearch() {
  document.getElementById('searchOverlay').classList.remove('open');
}

function doSearch(q) {
  const res = document.getElementById('searchResults');
  const query = q.toLowerCase().trim();
  const items = query ? searchData.filter(s => s.t.toLowerCase().includes(query) || s.d.toLowerCase().includes(query)) : searchData.slice(0, 12);
  res.innerHTML = items.map(s => `
    <div class="sr-item" onclick="location.href='${s.s}';closeSearch();">
      <span class="sr-tag" style="background:${s.c}22;color:${s.c}">${s.tag}</span>
      <span class="sr-title">${s.t}</span>
      <div class="sr-desc">${s.d}</div>
    </div>
  `).join('') || '<div style="padding:20px;text-align:center;color:var(--text-3)">No results</div>';
}

// Search trigger
document.getElementById('searchTrigger').addEventListener('click', openSearch);

// Search input handler
document.getElementById('searchInput').addEventListener('input', function() { doSearch(this.value); });

// Search overlay dismiss
document.getElementById('searchOverlay').addEventListener('click', function(e) { if (e.target === this) closeSearch(); });

// Ctrl/Cmd+K
document.addEventListener('keydown', e => {
  if ((e.metaKey || e.ctrlKey) && e.key === 'k') { e.preventDefault(); openSearch(); }
  if (e.key === 'Escape') closeSearch();
});

// ===== 41Q cell click to expand =====
document.querySelectorAll('.q-cell').forEach(function(cell) {
  cell.addEventListener('click', function() {
    this.classList.toggle('expanded');
  });
});

// ===== Scroll spy =====
const navLinks = document.querySelectorAll('.nav-link');
const sections = document.querySelectorAll('section[id]');
window.addEventListener('scroll', () => {
  let current = '';
  sections.forEach(s => { if (window.scrollY >= s.offsetTop - 100) current = s.id; });
  navLinks.forEach(l => {
    l.classList.remove('active');
    if (l.getAttribute('href') === '#' + current) l.classList.add('active');
  });
});

// ===== Fade in on scroll =====
const observer = new IntersectionObserver(entries => {
  entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('visible'); });
}, { threshold: 0.1 });
document.querySelectorAll('.fade-in').forEach(el => observer.observe(el));

// ===== Timeline =====
(function buildTimeline() {
  const daemons = [
    {name:'share-to-minty', type:'persistent', color:'#e5c07b'},
    {name:'corpus-agent', type:'persistent', color:'#61afef'},
    {name:'corpus-ingest-local', blocks:[[0,7.75],[20.25,24]], color:'#98c379', note:'20:15-07:45'},
    {name:'corpus-ingest-slack', blocks:[[8,20]], color:'#98c379', note:'08:00-20:00'},
    {name:'corpus-ingest-writer', type:'persistent', color:'#98c379'},
    {name:'slack-search', type:'persistent', color:'#c678dd'},
    {name:'news-pdf-bot', blocks:[[0,24]], color:'#c678dd', note:'Every 60s'},
    {name:'feed-promote', blocks:[[0,24]], color:'#56b6c2', note:'Every 60s'},
    {name:'cli-health-probe', blocks:[[0,24]], color:'#5c6370', note:'Every 10m'},
    {name:'rclone-sync', blocks:[[0,24]], color:'#5c6370', note:'Every 10m'},
    {name:'slack-index', blocks:[[0,1],[4,5],[8,9],[12,13],[16,17],[20,21]], color:'#c678dd', note:'Every 4h'},
    {name:'workspace-index', blocks:[[0,1],[6,7],[12,13],[18,19]], color:'#c678dd', note:'Every 6h'},
    {name:'feedme-daily', blocks:[[6,7]], color:'#56b6c2', note:'06:15'},
    {name:'yesterday-in-ai', blocks:[[5,6]], color:'#e06c96', note:'05:30'},
    {name:'last-week-in-ai', blocks:[[8,9]], color:'#e06c96', note:'Wed 08:00'},
    {name:'archive-feed-channels', blocks:[[12,13]], color:'#e5c07b', note:'Wed 12:00'},
    {name:'corpus-ingest-handoff', blocks:[[20,21]], color:'#98c379', note:'Daily 20:15'},
    {name:'ghost', type:'persistent', color:'#5c6370'},
    {name:'cloudflared', type:'persistent', color:'#5c6370'},
    {name:'guide-updater', blocks:[[6,7],[18,19]], color:'#5c6370', note:'06:00 & 18:00'},
  ];
  const c = document.getElementById('timelineChart');
  if (!c) return;
  daemons.forEach(d => {
    const row = document.createElement('div');
    row.className = 'timeline-row';
    row.innerHTML = `<div class="timeline-label">${d.name}</div><div class="timeline-bar-bg">
      <div class="hour-marks">${'<span></span>'.repeat(12)}</div>
      ${d.type === 'persistent'
        ? `<div class="t-block" style="left:0;right:0;background:${d.color}" title="Always running"></div>`
        : (d.blocks||[]).map(b => `<div class="t-block" style="left:${b[0]/24*100}%;width:${(b[1]-b[0])/24*100}%;background:${d.color}" title="${d.note||''}"></div>`).join('')
      }
    </div>`;
    c.appendChild(row);
  });
})();

// ===== Newsletter subscribe =====
document.getElementById('subscribe-form').addEventListener('submit', function(e) {
  e.preventDefault();
  const msg = document.getElementById('subscribe-msg');
  const btn = this.querySelector('button');
  const email = this.querySelector('input[name="email"]').value;
  const website = this.querySelector('input[name="website"]').value;
  btn.disabled = true;
  btn.textContent = 'Subscribing...';
  msg.textContent = '';
  msg.style.color = 'var(--text-2)';
  fetch('https://api.mintresearch.org/api/subscribe', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({email: email, website: website})
  })
  .then(function(r) { return r.json().then(function(d) { return {ok: r.ok, data: d}; }); })
  .then(function(res) {
    if (res.ok) {
      msg.style.color = 'var(--green)';
      msg.textContent = res.data.message === 'already subscribed' ? 'You\'re already subscribed!' : 'Subscribed! You\'ll get the next issue.';
      btn.textContent = 'Subscribed';
    } else {
      throw new Error(res.data.error || 'Something went wrong');
    }
  })
  .catch(function(err) {
    msg.style.color = 'var(--red)';
    msg.textContent = err.message || 'Failed to subscribe. Try again.';
    btn.disabled = false;
    btn.textContent = 'Subscribe';
  });
});

// ===== Cluster viz lazy-load =====
(function() {
  var vizFrame = document.getElementById('cluster-viz');
  var vizPlaceholder = document.getElementById('cluster-viz-placeholder');
  if (!vizFrame) return;
  var loaded = false;
  var vizObserver = new IntersectionObserver(function(entries) {
    entries.forEach(function(e) {
      if (e.isIntersecting && !loaded) {
        loaded = true;
        vizFrame.src = 'https://mint-philosophy.github.io/paper-map/?embed=true';
        vizFrame.onload = function() {
          vizFrame.style.opacity = '1';
          vizPlaceholder.style.display = 'none';
        };
        vizObserver.disconnect();
      }
    });
  }, { threshold: 0.1 });
  vizObserver.observe(vizFrame.parentElement);
})();

// ===== Sidebar structural dots =====
(function() {
  if (window.innerWidth <= 768) return;
  var sidebar = document.querySelector('nav.sidebar');
  if (!sidebar) return;
  var dotSections = ['content-pipeline', 'guide', 'agents', 'subscribe'];
  dotSections.forEach(function(secId) {
    var navLink = sidebar.querySelector('a[href="#' + secId + '"]');
    if (!navLink) return;
    var dot = document.createElement('div');
    dot.className = 'sidebar-dot';
    var rect = navLink.getBoundingClientRect();
    dot.style.top = (rect.top + rect.height / 2 - 2) + 'px';
    document.body.appendChild(dot);
  });
})();

// ===== Token counter (scroll-based output token estimate) =====
(function() {
  var counter = document.getElementById('tokenCounter');
  var countEl = document.getElementById('tokenCount');
  if (!counter || !countEl) return;
  var main = document.querySelector('main');
  if (!main) return;
  // Pre-compute total text length for token estimation
  var totalChars = main.textContent.length;
  var lastToken = 0;
  function updateTokens() {
    var scrollTop = window.pageYOffset || document.documentElement.scrollTop;
    var docHeight = document.documentElement.scrollHeight - window.innerHeight;
    if (docHeight <= 0) return;
    var ratio = Math.min(scrollTop / docHeight, 1);
    // ~4 chars per token (rough GPT/Claude tokenizer approximation)
    var tokens = Math.round((totalChars * ratio) / 4);
    if (tokens !== lastToken) {
      lastToken = tokens;
      countEl.textContent = tokens.toLocaleString();
    }
    if (scrollTop > 200) {
      counter.classList.add('visible');
    } else {
      counter.classList.remove('visible');
    }
  }
  var ticking = false;
  window.addEventListener('scroll', function() {
    if (!ticking) {
      requestAnimationFrame(function() { updateTokens(); ticking = false; });
      ticking = true;
    }
  });
  updateTokens();
})();

// ===== Section indicator bar (shows current section while scrolling) =====
(function() {
  var indicator = document.getElementById('sectionIndicator');
  var siNum = document.getElementById('siNum');
  var siName = document.getElementById('siName');
  var siCli = document.getElementById('siCli');
  if (!indicator) return;
  var headers = document.querySelectorAll('h2.cli-call[data-section-name]');
  if (!headers.length) return;
  var sections = [];
  headers.forEach(function(h) {
    var sec = h.closest('section');
    var num = sec ? sec.querySelector('.section-num') : null;
    var cliLine = h.querySelector('.cli-line');
    sections.push({
      el: sec || h,
      num: num ? num.textContent.trim() : '',
      name: h.getAttribute('data-section-name'),
      cli: cliLine ? cliLine.textContent.trim() : ''
    });
  });
  var current = '';
  function updateIndicator() {
    var scrollY = window.pageYOffset + window.innerHeight * 0.3;
    var active = null;
    for (var i = sections.length - 1; i >= 0; i--) {
      if (sections[i].el.offsetTop <= scrollY) { active = sections[i]; break; }
    }
    if (active && active.name !== current) {
      current = active.name;
      siNum.textContent = active.num ? '[' + active.num + ']' : '';
      siName.textContent = active.name;
      siCli.textContent = active.cli;
    }
    if (window.pageYOffset > 400) {
      indicator.classList.add('visible');
    } else {
      indicator.classList.remove('visible');
    }
  }
  var ticking2 = false;
  window.addEventListener('scroll', function() {
    if (!ticking2) {
      requestAnimationFrame(function() { updateIndicator(); ticking2 = false; });
      ticking2 = true;
    }
  });
  updateIndicator();
})();
</script>
</BaseLayout>
